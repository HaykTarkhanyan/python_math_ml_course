title = "ch09-10: Fashion-MNIST swap, UMAP lecture, harder eigenfaces project, and renumbering"
date = "2026-08-16"
time = "1515"
areas = ["ml/09_clustering", "ml/10_dimensionality_reduction", "_learnings", "DECISIONS.md", "ml/00_plan.md"]

done = """
Four pieces of work on the last two classic-ML chapters, all instructor-directed.

1. RUNNING DATASET: 8x8 digits -> Fashion-MNIST (DECISIONS #19).
   - New py_src/fetch_fashion_mnist.py reads torchvision's raw IDX files with pure numpy (no
     torch import) and caches a 12,000-image stratified subsample to data/fashion_mnist.npz
     (5.3 MB, committed). OpenML was down with a 504 during the session, hence the torchvision
     mirror.
   - dimred_demos.py rewritten around it. Verified numbers now on the slides: PC1 = 29.0%,
     PC1+PC2 = 46.8%, 95% variance at k=184 of 784, reconstruction cum-var 61.7/78.6/86.3%
     at k = 5/20/50.
   - Also swept REVIEW.md (dated 2026-07-07, never applied). All ten items done except #10,
     which was deliberately declined. Its item 1 - the deck claimed "first PC ~12%, first two
     ~22%" while its own scree figure showed 0.148 - had been live in the .tex for 13 months.
   - New figures: dr_scaling_trap (the standardize-or-not trap, show-don't-tell), 3-panel
     swiss roll with the actual 3-D roll, dr_clip_atlas (CLIP embeddings drawn as photographs).
   - Four digit-era figures moved to fig/_retired_digits/ rather than deleted.

2. NEW LECTURE 35_umap.tex, 29 pages (DECISIONS #20).
   - Ported from the instructor's own LMU student-assistant deck, copied to
     _reference_umap_lmu/ (gitignored - see 'pending').
   - Four overlapping frames compressed into one bridge frame; the LMU hyperparameter
     TikZ mock-ups became real UMAP runs on Fashion-MNIST (py_src/umap_demos.py).
   - Fixed two genuine math bugs in the source deck's toy example, both recorded in
     _learnings: the sigma binary search has no solution at k=2, and none either when two
     neighbours tie at rho. Toy now runs at k=3 with off-centre apexes.

3. NEW HOMEWORK PROJECT 2: "When eigenfaces meet the real world" (DECISIONS #21).
   - LFW instead of Olivetti; break it, diagnose it, fix it, be honest about it.
   - The entire arc was RUN before being assigned
     (py_src/non_essential/validate_lfw_project.py, which is the answer key):
     Olivetti 0.920 -> LFW 0.571 against a 0.411 majority baseline; corr(PC1, image
     brightness) = +0.998 with PC1 at 20.4% of variance; drop-3-PCs 0.655; standardizing
     pixels 0.575 (the intended disappointment); Fisherfaces 0.820; drop-3 + LDA also 0.820.

4. RENUMBERING both chapters into the global sequence (DECISIONS #22).
   - ml/ch4_clustering -> ml/09_clustering, ml/ch4b_dimensionality_reduction ->
     ml/10_dimensionality_reduction; qmds renamed to match their folders; decks and notebooks
     renumbered 32-36 continuing from time series (which ends at 31).
   - The three clustering practicals share 33 (one practical session, matching the 21_* precedent
     in ch04). Stray L13_baseline_tmp.vrb build artifact deleted.
   - Swept 32 files of cross-references. Student-visible callbacks in ch8_autoencoders now say
     "the dimensionality-reduction chapter" instead of "(ch4b)" - deliberately NOT "ch10", since
     ml/ch10_diffusion still exists and would be ambiguous.
   - ml/syllabus.csv DELETED at instructor request; ml/00_plan.md is the sole registry, and its
     stale time-series numbers were corrected from 30/31/32 to the 29/30/31 actually on disk.
   - Recompiled all four affected decks (32, 34, 35, and L22_autoencoders) and confirmed the
     cross-chapter CLIP data path in dimred_demos.py still resolves.

VERIFICATION: both decks compiled twice, rendered to PNG, and checked with a programmatic
bottom-edge overflow detector plus visual inspection of every changed frame. Five real
overflows found and fixed this way (the caveats box, the CLIP atlas frame twice, the sigma
frame, the symmetrization TikZ overlapping its text column, the 35_umap hook attribution).
One wrong caption caught by looking at the borrowed figure: the PAIR elephant shows one 3-D
cloud and one 2-D projection, not "four settings" as first written. One claim verified
numerically rather than asserted: sandal/sneaker/ankle-boot really are mutually each other's
two nearest classes in the t-SNE embedding, and so are pullover/coat/shirt.
"""

pending = """
- _reference_umap_lmu/ is GITIGNORED (.gitignore has `_reference*/`), matching the convention
  for borrowed source material. But this is the instructor's OWN deck, not a third-party
  video still. Decide whether to force-add it so the 35_umap source is version controlled.
- data/fashion_mnist.npz is 5.3 MB and not yet committed - confirm that is acceptable in the
  repo before pushing.
- Neither deck has been delivered, so 10_dimensionality_reduction.qmd still carries the "not finished"
  warning banner and the Google Form line is still TBD.
- Nothing committed this session. Working tree also still holds the earlier clustering
  land-cover practical work.
"""

next = """
- Review the two decks end to end (both PDFs open cleanly; 37 and 29 pages).
- Consider the opt-in Sonnet student review on 35_umap, since it is a brand new deck and the
  toy-example arithmetic is dense.
- Decide the two 'pending' items above, then commit.
- Optional: 34_dimensionality_reduction's swiss-roll UMAP panel does not lay the sheet out straight (no UMAP setting
  tried does). The caption is now honest about this - topology preserved, geometry not - but
  if a straight strip is wanted for teaching, Isomap is the method that produces one.
"""

files = [
  "ml/10_dimensionality_reduction/34_dimensionality_reduction.tex",
  "ml/10_dimensionality_reduction/35_umap.tex",
  "ml/10_dimensionality_reduction/35_umap_OUTLINE.md",
  "ml/10_dimensionality_reduction/10_dimensionality_reduction.qmd",
  "ml/10_dimensionality_reduction/py_src/fetch_fashion_mnist.py",
  "ml/10_dimensionality_reduction/py_src/dimred_demos.py",
  "ml/10_dimensionality_reduction/py_src/umap_demos.py",
  "ml/10_dimensionality_reduction/py_src/non_essential/validate_lfw_project.py",
  "ml/10_dimensionality_reduction/py_src/non_essential/README.md",
  "ml/10_dimensionality_reduction/data/fashion_mnist.npz",
  "ml/10_dimensionality_reduction/fig/",
  "DECISIONS.md",
  "_learnings/2026-08-16-1530_lstlisting-in-beamer-needs-a-fragile-frame.md",
  "_learnings/2026-08-16-1545_umap-sigma-search-has-no-solution-at-k-2.md",
  "_learnings/2026-08-16-1610_a-clean-embedding-makes-a-terrible-image-atlas.md",
]

commits = []

notes = """
The instructor stepped away mid-session with "decide for yourself and record your decisions".
Everything after that point was decided unilaterally and written into DECISIONS.md #19/#20/#21
so the reasoning is reviewable rather than buried in this log.

Three judgement calls worth flagging on return:
1. REVIEW.md item 10 (a 2x2 characteristic-polynomial worked example) was declined. The
   by-hand PCA frame already carries real covariance and eigenvalue numbers, and the deck is
   already 37 pages.
2. The CLIP atlas frame deliberately uses min_dist=0.8 rather than the default. At the default
   CLIP separates the ten classes so cleanly that every thumbnail stacks into ten dots. This is
   legitimate (min_dist changes packing, not neighbours) and is exactly the point 35_umap's
   min_dist frame makes, but it is a figure tuned for legibility rather than for analysis.
3. Project 2's task 8 asks students to try a fix that mostly does not work (standardizing
   pixels, 0.575 vs 0.571). That is intentional and the qmd warns "at least one of them will
   disappoint you", but it will generate questions.
"""
