title = "Project 3 given-code tooling + genes-mirror-geography practical, twice reviewed"
date = "2026-09-03"
time = "0245"
areas = ["ml/10_dimensionality_reduction", "ml/00_plan.md", "DECISIONS.md", "_learnings"]

done = """
PROJECT 3 (the assigned ch10 homework) restructured per instructor: the ML is the task, the
engineering is given.
- py_src/photo_map.py - make_photo_map(): 2-D points + thumbnails -> self-contained hover-map
  HTML in one call. Browser-verified with Playwright (traces, legend, hover thumb, no JS errors).
- py_src/embed_my_photos.py - ~60-line minimal CLIP embed of a student's own photo folder,
  npz layout-compatible with the chapter data. Tested end-to-end (transformers 5.x returns a
  model output from get_image_features - .pooler_output, same as the instructor script).
- qmd: assigned-project callout (P3 for everyone, P1 warm-up, P2 hard mode), given-code
  section, task 5 = matplotlib scatter + one provided call, task 7 = own-photos portfolio
  finale (GitHub Pages, no faces required).

GENES MIRROR GEOGRAPHY practical (instructor-led, DECISIONS #32) - built, validated, reviewed:
- fetch_1000g_genotypes.py: streams 1000G phase 3 chr22 (~205MB, URL HEAD-verified), keeps
  common biallelic SNPs -> data/genomes_1000g_chr22.npz (2504 x 16202 int8, 6.0MB, numpy-only).
- Pre-registered validation (validate_genes_geography.py) BEFORE writing the notebook:
  world silhouette 0.588, kmeans ARI 0.87; EUR = FIN-vs-mainland only; 16k-vs-97k measured -
  IBS-TSI separation grows ~sqrt(6) and stays invisible -> committed npz stays 6MB.
- genes_geography_maps.py (geography|PCA side-by-sides, Natural Earth coastlines, official
  1000G population names) + genes_geography_nonlinear.py (PCA|t-SNE|UMAP, silhouette table:
  0.588/0.591/0.712 super-pops, all-negative for 26 pops).
- xx_genes_geography_solution.ipynb (24 cells, executed clean, embeddings cached for live):
  Act 0 matrix -> Act 1 world PCA + scaling measured -> Act 2 Europe with a REAL caught
  NaN crash (323 EUR-monomorphic SNPs) -> Act 3 pretty-vs-true (UMAP tucks the admixture
  continuum into a tongue; Chari & Pachter 2023) -> Act 4 held-out projection 80% (2 PCs,
  14% var) -> 93% (10 PCs, 19% var), residual errors = admixture.

TWO REVIEWS: my inline pass (2 markdown nits), then one adversarial subagent that reproduced
every number digit-for-digit and found: ASW/ACB mislabeled as "orange smear" members (AFR/red),
Novembre "500k SNPs" -> ~197k after QC, sqrt(p(1-p)) wording, -16-degree rotation omission,
rare-variant rationale backwards, rng-reuse footgun, Europe label collisions. All fixed,
re-executed, numbers unchanged. Its silence about the wrap-up section exposed that my earlier
patch had DELETED the wrap-up cell - restored (see learning below).
"""

pending = """
- Untracked and deliberately NOT committed: ml/claude_projects/ (54MB - the 48MB corpus needs
  a gitignore-vs-commit decision), app_final.png (root stray from Aug 21), tmp/ (scratch),
  ml/10_dimensionality_reduction/out/photo_map_demo.html (8.5MB regenerable demo).
- Numbering drift decision still open (ch10 files 35/36/37 vs YouTube [36]/[37]); the
  practical keeps its xx_ prefix until then, and gets qmd-linked at delivery.
- update-youtube registry refresh; ch10 Random link + Google Form TBDs.
"""

next = """
- Deliver the practical (embeddings cache committed, so the slow path never runs live).
- Then the deep-learning half: neural networks (L14, L15).
"""

files = [
  "ml/10_dimensionality_reduction/xx_genes_geography_solution.ipynb",
  "ml/10_dimensionality_reduction/data/genomes_1000g_chr22.npz",
  "ml/10_dimensionality_reduction/py_src/fetch_1000g_genotypes.py",
  "ml/10_dimensionality_reduction/py_src/genes_geography_maps.py",
  "ml/10_dimensionality_reduction/py_src/genes_geography_nonlinear.py",
  "ml/10_dimensionality_reduction/py_src/non_essential/validate_genes_geography.py",
  "ml/10_dimensionality_reduction/py_src/photo_map.py",
  "ml/10_dimensionality_reduction/py_src/embed_my_photos.py",
  "ml/10_dimensionality_reduction/10_dimensionality_reduction.qmd",
  "DECISIONS.md",
  "_learnings/2026-09-03-0530_replacing-a-cells-source-can-swallow-its-roommates.md",
]

commits = [
  "ml(ch09): semantic-tree practical, colour figures, session-log backlog",
  "ml(ch10): lectures 35-36 delivered; review round 2 applied; Project 3 given-code tooling",
  "ml(ch10): genes-mirror-geography practical on 1000 Genomes chr22",
]

notes = """
Learning of the session (2026-09-03-0530): replacing a notebook cell's whole source can
swallow other content sharing the cell; a reviewer's OMISSIONS are evidence. Also recorded
in-notebook: the practical's every claim is printed by the notebook itself.
"""
