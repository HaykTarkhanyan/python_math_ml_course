title = "ch19 mechanistic interpretability: 3 decks -> 8 (intro, 3 core revisions, 4 optional add-ons), heavy runs on Colab"
date = 2026-09-22
time = 21:13:00
areas = ["ml/ch19_mech_interp", "ml/ch19_mech_interp/py_src", "ml/ch19_mech_interp/results", "ml/ch19_mech_interp/fig", "non_essential", "DECISIONS.md"]
tags = ["mech-interp", "gpt2", "resnet18", "causal-tracing", "rome", "feature-visualization", "model-diffing", "colab", "autonomous"]
done = [
  "Interview-locked spec (EXTENSION_PLAN.md, DECISIONS #36/#37): house decks, all four gap topics, GPT-2 small + attn-only toys + ResNet-18, own fine-tuned model organism, core (4) + droppable add-ons (4), commit per deck, no push. L45-L47 renamed to xx_ names until delivery",
  "Core: NEW intro 'Looking Inside' (41 frames; toy 4-neuron MLP read by hand, gate G1); 'Opening the box', 'Does it actually do that?', 'Features' revised easier-first with by-hand toys, logit lens on a second task, measured shuffled-label probe control, Jordan/Brady easy patch, worked superposition example",
  "Add-on 'Circuits read off the weights' (33 frames): QK/OV, bigrams from W_E W_U, one vs two layers on repeated sequences (0% vs 65%), composition scores (gate G4), eigenvalue copying score shown disagreeing with the direct test",
  "Add-on 'Where facts live' (24 frames): MLP as key-value table, causal tracing on 13 known athlete facts (2x noise, 3-layer window, noise table on its own frame - DECISIONS #42), rank-one edit from scratch (Jordan -> tennis 95%, 7 of 12 other athletes kept, Bulls 95 -> 11%), same edit at every layer: traced layer 0 is the worst place to edit",
  "Add-on 'Seeing what a network sees' (25 frames): saliency / SmoothGrad / IG with completeness converging (+0.03% at 512 steps), Adebayo sanity check (guided backprop 0.97 with a random classifier), feature visualisation with naive-vs-regularised pair, curve detector layer2 ch23 (arc 4.42 vs line 1.15) built from tangent-oriented edge detectors, crude polysemanticity count with its false positive named",
  "Add-on 'Personas and model diffing' (20 frames): GPT-2 small fine-tuned on 640 positive SST-2 sentences got more positive about everything (13/13 non-movie prompts); weights moved evenly, activation shift aligns with a base-model sentiment direction; the direction is a weak monitor (47-62%) but a strong lever, and pinning it removes only ~56% at one layer - framed as sufficient but only partly necessary",
  "Heavy runs moved to a Colab T4 via the colab CLI in WSL after the laptop hit 100% CPU / 97% RAM (DECISIONS #41); all five heavy scripts pick cuda when present and redraw figures on CPU with --plot-only; Colab session stopped",
  "Bugs found and fixed: IG baseline 1x3x1x1 scored as a 1x1 image (completeness off 6-17%); feature viz flat grey (post-ReLU zero gradient -> pre-activation objective); HF/TL to_tokens silent 1024 truncation (truncate=False, caught by an assert); float32 rounding + indent=2 made vision JSONs 30 MB (now 5.4 MB, fixed before any push)",
  "Every add-on figure redrawn at its slide-slot size (fonts 7-9pt); all decks 0 errors, 0 clipped, 0 footer collisions; mech_interp.qmd has Armenian entries for all 8 decks and an honest Code section (core on CPU, add-on heavy runs on Colab)",
]
pending = [
  "Opt-in Sonnet 'student review' of the rendered decks (one subagent per deck, ask first) - none run yet; the four add-ons and the intro are the ones that most need a first-time-reader pass",
  "The fine-tuned GPT-2 checkpoint existed only on the stopped Colab VM; rerunning diffing_finetune.py retrains it (~80 steps on a T4). The committed JSON is the artifact of record",
  "Decide which add-ons get a session; drop the others from mech_interp.qmd (do not delete the decks)",
  "Out of scope, still unbuilt: HW_mech_interp.ipynb; the chapter's slot in ml/00_plan.md",
  "git warned 'garbage found: .git/objects/bc/tmp_obj_lWbA3G' - a stray temp object from an interrupted git write, not from this session's commits; harmless, `git gc` would remove it",
]
next = [
  "Pick the add-ons to teach; if all four, the order is intro, vision, opening the box, circuits, causal, facts, features, personas (each add-on needs only core decks before it)",
  "On delivery: rename xx_ decks to NN_ numbers, link notes PDFs and videos in mech_interp.qmd, drop the 'not finished' warning",
]
files = [
  "ml/ch19_mech_interp/EXTENSION_PLAN.md (spec, gates G1-G7 all answered, build log)",
  "ml/ch19_mech_interp/xx_mech_interp_intro.{tex,pdf}, xx_opening_the_box.{tex,pdf}, xx_does_it_actually_do_that.{tex,pdf}, xx_features.{tex,pdf}",
  "ml/ch19_mech_interp/xx_transformer_circuits.{tex,pdf}, xx_where_facts_live.{tex,pdf}, xx_vision_circuits.{tex,pdf}, xx_personas_and_diffing.{tex,pdf}",
  "ml/ch19_mech_interp/py_src/: mi_common.py (pick_device, save_results compact), intro_toy_mlp.py, intro_figs.py, l45_easy_figs.py, l46_easy_figs.py, circuits_figs.py, facts_tracing.py, facts_rome.py, vision_attribution.py, vision_features.py, diffing_finetune.py; py_src/archive/ (4 probes + README)",
  "ml/ch19_mech_interp/results/*.json and fig/*.pdf for every script above",
  "non_essential/deck_contact_sheet.py (+ README row)",
  "ml/ch19_mech_interp/mech_interp.qmd, DECISIONS.md (#36, #37, #41, #42; #13 superseded)",
]
commits = ["6f0f7cd", "d693503", "d8cb8f1", "30d0889", "19c288a", "86f1fa7", "ee7e025", "9dddc62", "560662d", "53b90e1"]
notes = '''
Measure before writing paid off in every add-on, and several drafts had to be rewritten to match
the data: the Eiffel Tower hook (GPT-2 small does not know it), the 3x tracing noise (washes the
picture out on a 124M model), the "predict first" in the vision deck (the striking result is
guided backprop ignoring a random TOP of the network, not full randomisation), and the personas
monitor frame (the draft claimed the direction separates unseen sentences; measured, it is near
chance - which became the point of the last section).

Two mistakes repeated from earlier in the build: figures drawn wider than their slot (text at
4-5pt again, fixed by sizing every figure to its slot), and patch scripts passed through a bash
heredoc, which collapses \\ to \ in this shell (it corrupted \circ into a tab once; caught
and repaired). Patches written with the Write tool, or edits via Edit, avoid it.

The vision commit was amended (still local, never pushed) so the 25 MB JSON never enters branch
history. That is the only amend in this session.
'''
