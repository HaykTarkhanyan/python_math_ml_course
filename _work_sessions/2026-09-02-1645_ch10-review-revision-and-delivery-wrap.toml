title = "Ch10 review round 2 + pedagogy frames, then delivery wrap (notes, videos, qmd)"
date = "2026-09-02"
time = "1645"
areas = ["ml/10_dimensionality_reduction", "ml/00_plan.md", "DECISIONS.md", "_learnings"]

done = """
REVIEW ROUND 2 of both ch10 decks (round 1 of 2026-07-07 was already fully applied), then
applied everything same-session (DECISIONS #31, REVIEW.md round-2 section):

35_dimensionality_reduction.tex (37 -> 41 pages):
- Instructor-requested pedagogy: NEW frames "Why maximize variance?" (dr_why_variance.pdf,
  projected |A-B| = 12.6 vs 0.7), covariance-matrix refresher (Var(Xw) = w'Sigma-w identity
  derived), "What PCA does to a point - and what linear means" (dr_linear_grid.pdf, a grid a
  matrix cannot bend), SVD frame rebuilt around an annotated U/S/V' block diagram (Z = US),
  eigen-garments frame (dr_eigengarments.pdf, PC1 29% / PC2 18%, sets up Project 1).
- Review items: "New points?" decision-table column (t-SNE has no out-of-sample map);
  LDA/Fisherfaces pointer on "When NOT to reduce"; PCA-inside-the-pipeline leakage clause;
  crowding problem explained; elbow callback to ch09; stale L13c refs fixed; acronym pass
  (t-SNE, UMAP, LSA, EVR, DR all previously unexpanded - the SLIDE_STYLE.md grep finally run).

36_umap.tex (29 -> 30 pages):
- NEW frame "Where does the layout start?": spectral init + honesty box - Kobak & Linderman
  (2021, Nat. Biotech.) attribute much of UMAP's global-structure edge to initialization;
  sklearn t-SNE defaults to PCA init since 1.2 (both web-verified). Payoff box + recap softened.
- "From L13b" fixed; cosine-metric why; Kobak & Linderman added to references.

Figures: dimred_demos.py gained fig_why_variance / fig_linear_grid / fig_eigengarments and a
CLI name-filter (single-figure runs skip the multi-minute t-SNE). All rendered + eyeballed;
3 layout collisions and 2 overflows caught at render and fixed.

DELIVERY WRAP (2026-09-03, lectures delivered, instructor exported _notes.pdf by hand):
- Clean PDFs recompiled twice + cleaned (35: 41pp, 36: 30pp; the 20:56 single-pass recompile
  after clean_latex would have left a blank Outline - caught and redone). _notes.pdf untouched.
- qmd: sunset random image, "delivered" callout, Armenian per-lecture summaries with
  slides + notes + exact-YouTube-title links ([36] ZYS67FCMTUo, [37] 1-3MnkG1wso).
- 00_plan.md: both rows marked delivered; classic-ML-track-complete milestone marked reached;
  numbering-drift note added (YouTube [36]/[37] vs disk 35_/36_ - see pending).
"""

pending = """
- NUMBERING DRIFT (decision needed): ch09 practicals became separate videos [34]/[35], so the
  ch10 files 35_/36_/37_ trail the playlist by one ([36]/[37]/[38?]) and 35_ is now used by two
  chapters. Renumber ch10 files to 36/37/38 per convention, or accept the drift? DECISIONS #22
  anticipated exactly this case.
- _meta/youtube_channel.md registry predates even the ch09 videos - needs an update-youtube
  refresh pass.
- Chapter qmd TBDs: Random link of the week, Google Form.
- Nothing committed yet - working tree also still carries the earlier ch09 renames.
- Optional: Sonnet student-review of the revised decks (opt-in, never run automatically).
"""

next = """
- Ch10 practical session: Project 1 eigenfaces (Olivetti), Project 2 LFW break/diagnose/fix
  (Fisherfaces), Project 3 CLIP photo-grouping. Solutions: 37_eigenfaces_solution.ipynb,
  37_image_clusters_solution.ipynb; LFW arc pre-validated (DECISIONS #21).
- Then the deep-learning half: neural networks (L14, L15).
"""

files = [
  "ml/10_dimensionality_reduction/35_dimensionality_reduction.tex",
  "ml/10_dimensionality_reduction/36_umap.tex",
  "ml/10_dimensionality_reduction/py_src/dimred_demos.py",
  "ml/10_dimensionality_reduction/fig/dr_why_variance.pdf",
  "ml/10_dimensionality_reduction/fig/dr_linear_grid.pdf",
  "ml/10_dimensionality_reduction/fig/dr_eigengarments.pdf",
  "ml/10_dimensionality_reduction/10_dimensionality_reduction.qmd",
  "ml/10_dimensionality_reduction/REVIEW.md",
  "ml/00_plan.md",
  "DECISIONS.md",
  "_learnings/2026-09-02-1705_documented-checks-dont-run-themselves.md",
]

commits = []

notes = """
The honesty-box decision (don't oversell the repulsion-term story) is DECISIONS #31; if it
confuses in delivery, demote to a spoken remark. Learning of the session: checks that live as
prose in a style guide don't run - the acronym grep and the rename sweep both missed things a
one-line script would have caught.
"""
