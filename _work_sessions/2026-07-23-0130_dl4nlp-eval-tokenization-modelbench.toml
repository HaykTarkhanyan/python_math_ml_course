# Work session log. See _work_sessions/README.md for the schema.

title = "DL4NLP: eval-deck polish + student-review incorporation, tokenization fixes, and a Haiku/Sonnet/Opus reviewer bake-off"
date = 2026-07-23
time = 01:30:00            # approximate session start (local); ran to ~03:00
session_name = "dl4nlp_based_slides"
areas = ["misc/dl4nlp", "_work_sessions", ".claude/skills/wrap-session", "(global CLAUDE.md)"]
tags = ["dl4nlp", "beamer", "slides", "evaluation", "tokenization", "agents", "model-comparison", "workflow"]

done = [
  "Eval deck (05_evaluation): reviewed and fixed 5 overlaps/overflows (BERTScore split into matrix + trade-offs, LLM-as-Judge split into pointwise/pairwise + biases, benchmark-table split into two, BLEU brevity-penalty label, landscape spacing) and added 5 frames for harder topics (perplexity worked example, BERTScore similarity matrix, LLM pointwise-vs-pairwise, benchmark contamination). 20 -> 25 pages; compiles clean; every changed page eyeballed for overflow.",
  "Eval deck: ran a Haiku student-review agent on the rendered slides and incorporated ~13 of its questions as in-slide clarifications (PP = effective branching factor; named H as cross-entropy; reconciled BLEU r/c vs the graph's c/r; added a ROUGE-L LCS definition + example; BERTScore 'P != R, equal here by coincidence' note; explained canary strings; guessable = 25 percent for free; solved = passed the human baseline; zero p_n wipes out BLEU -> smoothing).",
  "Tokenization deck (03_tokenization): fixed the p9 title/corpus overlap, and REBUILT the p13 WordPiece worked example which had WRONG arithmetic - corrected the (e,e) count 18 -> 25, (e,p) 12 -> 7, freq(e) 43 -> 65, and the scores to 0.0059 / 0.0090 - and fixed its severe header/formula overlap. Compiles clean, 22 pages. (The BPE example on p9 was already numerically correct: merges 23/23/18 verify.)",
  "Model bake-off: ran the IDENTICAL student-review prompt on the tokenization deck with Haiku, Sonnet, and Opus (one general-purpose agent each, same rendered slides). Winner = Sonnet: 0 false positives, caught the most real bugs INCLUDING both layout clips, mid cost (58k tok / 297s). Haiku: 2 vision misreads (disqualifying for QA you act on) (37k / 135s). Opus: 0 FP + unique conceptual catches but MISSED both layout clips and is the priciest per token (44k / 284s); best as a premium second pass. Decision: default to Sonnet for deck QA; Sonnet + Opus for a flagship deck; never Haiku for findings acted on unchecked.",
  "Convention: added time to the work-session convention - filename now YYYY-MM-DD-HHMM (24h local, no colon) plus a `time` field. Updated _work_sessions/README.md, the wrap-session skill (step 3), and the global CLAUDE.md.",
]

# The 13 tokenization issues surfaced by the bake-off, to fix LATER (deferred by instructor):
pending = [
  "1. Slide 6: 'Harder long-range dependencies' bullet is clipped by the verdict box below it (real overlap).",
  "2. Slide 21: the three further-reading boxes overlap, hiding the Kudo 2018 and Wang 2020 citations (real overlap).",
  "3. Slide 16: character count says 39, should be 40 (33 letters + 7 spaces, per the slide-6 convention).",
  "4. Slide 5: output shows 4 [UNK] but only 3 words are highlighted; explain the capitalized 'The' (case-sensitivity vs lowercase 'the' in vocab).",
  "5. Slide 6: the worked example is 23/7 ~ 3.3x, contradicting the '~4-5x longer' bullet on the same slide.",
  "6. Slides 12 vs 16/19: inconsistent 'unbelievably' WordPiece split - 3 pieces (un + ##believ + ##ably) on slide 12, 5 pieces (un + ##bel + ##ie + ##va + ##bly) on 16/19. Make 12 match real BERT or label it illustrative.",
  "7. Slide 9: acknowledge the (c,h)/(h,e) tie at 23 - BPE breaks ties by merge order / first-seen; the slide silently picks (c,h).",
  "8. Slides 4/6/18: 'character ~ 256' conflates Unicode characters with byte values (256 = 2^8 bytes), which also contradicts slide 11's reason for going byte-level.",
  "9. Slide 11: state that GPT-2's 50,257 = 256 bytes + 50,000 merges + 1 special end-of-text token (the +1 is missing, so 256 + 50000 = 50256 doesn't reproduce).",
  "10. Slides 14/20: define 'pre-tokenization' - used as a selling point on both slides but never defined.",
  "11. Slide 10: token IDs [9,4,3] only reproduce if you assume the slide-9 vocab-list order; state the indexing convention or show a small vocab->ID table.",
  "12. Slide 12/13: derive (or at least name) why freq(ab)/(freq(a)*freq(b)) corresponds to 'maximize corpus likelihood' - it is PMI-like.",
  "13. Slide 14: define the Unigram LM mechanism (how it segments a word / scores likelihood); optionally tie together the ## continuation marker (WordPiece) vs the space-prefix marker (GPT-2).",
]

next = [
  "Do the 13 tokenization fixes above - start with the two overlaps (slides 6, 21) + the numeric/consistency errors (slides 16, 6, 11), then the clarifications.",
  "Commit the uncommitted work: eval deck (05), tokenization deck (03), and the convention files (README + wrap-session skill). The global CLAUDE.md is outside the repo.",
]

files = [
  "misc/dl4nlp/05_evaluation.tex",
  "misc/dl4nlp/05_evaluation.pdf",
  "misc/dl4nlp/03_tokenization.tex",
  "misc/dl4nlp/03_tokenization.pdf",
  "_work_sessions/README.md",
  ".claude/skills/wrap-session/SKILL.md",
  "(global) ~/.claude/CLAUDE.md",
]

commits = []

notes = '''
Nothing committed this session - the eval deck, tokenization deck, and convention files
are all uncommitted in the working tree. (The prior shared-preamble + sectioning work IS
committed: e748acb workflow migration, bd07f2f dl4nlp preamble+sections.)
This session continues 2026-07-23_dl4nlp-shared-preamble (same long session after /compact).
Times are approximate: start ~01:30 local, this log written ~02:57.
Bake-off raw stats (tokenization deck, 22 slides, identical prompt):
  Haiku  37k tokens / 135s / 2 false positives (vision misreads).
  Sonnet 58k tokens / 297s / 0 FP / caught both layout clips -> WINNER.
  Opus   44k tokens / 284s / 0 FP / missed both layout clips / priciest per token.
'''
