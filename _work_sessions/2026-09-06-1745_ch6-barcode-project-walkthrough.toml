title = "ch6 barcode-scanner project: measured, built, executed, registered"
date = "2026-09-06"
time = "1745"
areas = ["ml/ch6_cnn", "DECISIONS.md", "_learnings", "DEFERRED_TODO.md"]

done = """
NEW PRACTICAL: ml/ch6_cnn/project_barcode.ipynb (67 cells, executed clean, 3.8MB) - the
instructor's ask: EAN-13 bars-to-digits decoding, check digit, gradient-based orientation,
localization, everything explained slowly, OpenCV allowed. Instructor chose (asked before
they left): single walkthrough notebook (no TODO version), synthetic + real web photos,
standalone project naming (not HW1d).

Built measure-first (per _learnings/2026-08-13-2015): 8 scratch scripts proved every claim
before any prose was written. Key measured facts, all reproduced by the executed notebook:
- check digit: 0/35100 single-digit errors slip; 2490/21631 adjacent swaps slip, exactly
  the pairs differing by 5; backwards reads passing a forward parse: 0/500.
- robustness bench (30 codes x 13 degradations, frozen images, 6 methods): adaptive
  threshold fixes the ramp (0->30/30) but breaks noise rows global handled (30->0/30) ->
  checksum-gated cascade; sliding 59-run window fixes quiet-zone speckle (ramp+noise
  2/30 -> 30/30); band averaging crushes noise (sigma=120: 30/30); blur=module width is
  fatal at 3px modules, trivial at 6px (0/30 vs 30/30).
- orientation: structure tensor, 18/18 decodes across 0-170 deg, ~1 deg accuracy.
- localization: classic |gx|-|gy| is accidentally rotation-symmetric (convertScaleAbs abs)
  but dies near 45 deg (7/15); tensor coherence localizer 40/40.
- real photos (6 from Wikimedia Commons, committed to data/barcode/, downscaled, with
  _sources.json): ours 5/6 vs cv2.barcode.BarcodeDetector 3/6 (it misses coke can + milk
  carton that we read). Taiwan strawberry photo has its EAN in the Commons filename -
  exact ground-truth match. Spitfire bottle fails honestly (barcode wraps the bottle
  shoulder - information destroyed at sampling) and yielded a caught false positive
  (1-vote checksum fluke, killed by the min-3-votes gate) - both kept in as teaching acts.

REGISTERED: cnn.qmd resources + new Armenian '## Project' section between HW1c and
HW2-HW4. Logged: DECISIONS #35 (project shape/scope) and #34 (opencv pinned 4.11.0.86;
5.x forbidden - it silently upgraded numpy 1.26->2.4, rolled back; also a _learnings
file). DEFERRED_TODO: derive a student/TODO version if ever assigned as graded homework.

Verified after execution (per the write-after-measuring + swallowed-cell learnings):
all printed numbers diffed against prose (one fix: band+search prose ignored the
blur2+noise60 row), all 12 section headers present, 0 error outputs, 10 figures.

TWO REVIEWS (instructor asked 'pls review'): (1) my inline factual pass - 4 fixes: the
cv2.barcode 'WeChat scanning team' attribution was wrong (added in OpenCV 4.5.3; only the
optional super-resolution model is WeChat's), '30 fps' design claim softened to the
measured ms, QR sticker tolerance 20% -> ~30% at level H, 'Hong Kong Coke' -> 'code
issued in Hong Kong' (consistency with the registered-not-made caveat). (2) One
adversarial Sonnet subagent (~160k tokens): re-derived check-digit/parity/tensor math
independently (0 mismatches), verified figures against visible printed digits, flagged 9
findings. Applied: beer-can resolution footnote now DEMONSTRATED by a cell (2000px = 11
votes, 1400px = fails), read_barcode auto-converts color input (students' own photos),
new figure drawing the found boxes on coke + tea photos, quiet-zone 9-vs-spec-11/7
docstring note, 'generous box' expectation sentence, 'Spoiler: the bottle beats us',
'shoulder' -> 'curve of the bottle', n_lines 21-vs-41 explained. Declined: empirically
printing derivable arithmetic (1/10, sqrt(15), 64 parities). Rebuilt + re-executed
(70 cells, 2m35s, 0 errors), every previous number reproduced identically.
"""

pending = """
- NOTHING COMMITTED - instructor was away; everything left in working tree for review.
  New: project_barcode.ipynb, data/barcode/ (6 jpgs ~1.3MB + _sources.json),
  _learnings/2026-09-06-1815_*.md, this session file. Modified: cnn.qmd, DECISIONS.md,
  DEFERRED_TODO.md. Plus the pre-existing uncommitted ch10/etc. changes from earlier
  sessions (untouched).
- The notebook builder script lives in the session scratchpad only (build_nb.py) - the
  .ipynb is the artifact; regeneration would lose executed outputs anyway.
"""

next = """
- Instructor: review the notebook (open project_barcode.ipynb), then commit ch6 project
  as its own unit.
- Optional: photograph 2-3 Armenian products (485 prefix) and drop into data/barcode/ -
  the wrap-up section already points students there.
- HW2-HW4 for ch6 remain TBD (unchanged).
"""

files = [
    "ml/ch6_cnn/project_barcode.ipynb",
    "ml/ch6_cnn/data/barcode/",
    "ml/ch6_cnn/cnn.qmd",
    "DECISIONS.md",
    "DEFERRED_TODO.md",
    "_learnings/2026-09-06-1815_installing-opencv-5-silently-upgrades-numpy-to-2x.md",
]
commits = []

notes = """
- ma venv now has opencv-python==4.11.0.86 (numpy stayed 1.26.4 - see DECISIONS #34).
- Notebook runs locally (~3.5 min via ma) and is Colab-ready: photos download from the
  repo's raw GitHub URLs on first run (will 404 until pushed - local data/ works now).
- Facts web-verified during build: GS1 485=Armenia / 489=Hong Kong (the Coke can is a
  Hong Kong product), first UPC scan 1974-06-26 Wrigley's Juicy Fruit, check-digit
  transposition blind spot |a-b|=5.
"""
