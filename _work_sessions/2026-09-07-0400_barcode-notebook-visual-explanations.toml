title = "Barcode notebook round 2: every fix shown visually, gradients and energy/coherence taught properly"
date = "2026-09-07"
time = "0400"
areas = ["ml/ch6_cnn"]

done = """
Instructor asked (verbatim intent): show visually what each robustness fix actually does,
explain/show energy and coherence more, explain the crucial encode/decode functions
better, and explain the gradient parts better. project_barcode.ipynb grew 70 -> 94 cells
(5.7MB, executed clean, 0 errors); all previously verified numbers reproduced identically.

New content, all pre-measured in scratch before prose (s9_visuals_measure.py):

ENCODE/DECODE EXPLAINED:
- encoder trace cell: digit-by-digit table (position / digit / alphabet / modules) +
  a fully annotated barcode figure with every digit's 7-module span labelled digit/alphabet -
  the alphabet row literally spells the parity pattern LGLLGG -> hidden first digit 4.
- run-index map table (runs 0-2 guard, 3-26 left, 27-31 middle, 32-55 right, 56-58 guard).
- 'one digit under the microscope': runs 3-6 -> normalised widths -> top-3 nearest
  signatures printed + measured-vs-signature bar chart (winner dist 0.00, runner-up 2.00).
- decode_verbose(): narrating clone of decode_runs - all 12 digits with widths, matched
  digit/alphabet and distance, then the parity gate and the check-digit gate line by line.

EVERY FIX NOW HAS A PICTURE:
- voting: noise s=60 image with all 21 scanlines colored by fate - measured 1 green /
  20 red / 0 wrong votes; prose reframed around the real mechanism ('an election where
  every mistaken voter stays home'), which the old 'majority' framing had wrong.
- adaptive threshold: 3-panel figure (scanline + global line + rolling mean curve, then
  global vs adaptive binarization strips) for ramp 0.7 (global paints left half solid
  black, adaptive recovers bars) AND the mirror figure on noise s=30 (adaptive shatters
  into phantom flecks).
- band averaging: raw s=120 static scanline vs the 15-row mean where bars walk back out.
- window search: the crime-scene figure - band+adaptive binarized ramp+noise line has 65
  runs (printed; clean = 59) with the left quiet zone zoomed to show phantom micro-bars.

GRADIENTS TAUGHT SLOWLY (section 5 rebuilt, 2 cells -> 8):
- Sobel |gx| and |gy| maps of the rotated barcode first;
- quiver of gradient vectors on the strongest edges (one axis, both directions);
- printed mean gradient (+0.000, +0.000) vs mean magnitude 102.5 - the sign cancellation;
- orientation histogram: two spikes exactly 180 deg apart;
- THEN the structure tensor as the cure (square before averaging), formula + coherence.

ENERGY/COHERENCE DEEPENED (section 6):
- four-patch calibration study: barcode 416160/1.000, printed text 43985/0.137, cat fur
  4147/0.164, defocused corner 2662/0.301 - with the warning that low-energy coherence is
  noise (the least-structured patch has the highest non-barcode coherence). First crop
  attempt showed the cat's eye labelled 'fur' - re-cropped to the honest nose/fur patch.
- localization demo expanded 4 -> 6 panels: energy map, coherence map, raw AND mask,
  after close+open, final minAreaRect box (morphology stages now visible).

All new figures visually inspected after execution (annotated encoder, voting fate, ramp
3-panel, quiver, six-panel localization, patch gallery). Two nbconvert stumbles: one
kernel-startup timeout (transient, fixed with startup_timeout=180) and twice running
nbconvert from the scratchpad cwd (pattern matched no files - use the absolute path).
"""

pending = """
- Still nothing committed (instructor reviewing in IDE). The ch6 project unit now spans:
  project_barcode.ipynb, data/barcode/, cnn.qmd edit, DECISIONS #34/#35, learnings file,
  DEFERRED_TODO entry, three session logs.
"""

next = """
- Instructor: re-read the expanded notebook (94 cells), then commit as one unit.
- If the notebook now feels long for one sitting, natural split point is after section 4
  (decoding) / before section 5 (orientation + localization + real photos).
"""

files = ["ml/ch6_cnn/project_barcode.ipynb"]
commits = ["cd41740 ml(ch6): barcode-scanner project - EAN-13 from scratch, walkthrough notebook"]

notes = """
- The builder script (scratchpad build_nb.py) remains the notebook's source of truth for
  regeneration; every fix from both review rounds is synced into it.
- Notebook runtime is now ~4-6 min locally (was ~2.5) - the added figures are cheap, the
  variance is machine load.
"""
