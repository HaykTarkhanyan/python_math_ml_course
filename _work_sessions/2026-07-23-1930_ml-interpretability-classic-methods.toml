title = "Interpretability chapter LMU-alignment + student reviews; classic-methods LDA/QDA + GP"
date = 2026-07-23
time = 19:30:00
areas = ["ml/05_interpretability", "ml/04b_classic_methods", "ml/04_trees"]
tags = ["interpretability", "SHAP", "LIME", "PFI", "PDP", "H-statistic", "LDA", "QDA", "gaussian-process", "LMU-IML", "student-review", "full-bleed-embed"]

done = [
  "ml/04b_classic_methods deck (L12b): added LDA/QDA discriminant derivation (Gaussian density -> delta_k -> shared-Sigma cancellation => linear) as 2 new frames + covariance-ellipse figure (cm_lda_qda); added a GP conditioning frame (joint Gaussian -> posterior mean/var). Sonnet student review -> fixed SVM WLOG scale justification, pi_k gloss, GP conditioning-identity note.",
  "ml/05_interpretability deck 2 (02_pfi_and_effects): NEW 'main effects vs interactions' decomposition/definition frame; retitled PDP as the main-effect plot; NEW 'Feature interactions' section = 2-D PDP + Friedman H-statistic, with new figures pdp_2d_interaction.pdf (synthetic saddle) and h_statistic_bar.pdf (real bike, all H<0.2); reordered effects PDP->ALE->ICE->interactions; embedded LMU worked-PFI slide (lmu_pfi_worked.pdf).",
  "ml/05_interpretability deck 3 (03_shap_lime): embedded LMU Shapley computation slides (lmu_shapley_value.pdf, lmu_shapley_formula.pdf); authored Adi/Budi/Gogi coalition worked example (names via qarakusi.am) -> phi=(20,30,40) summing to 90; added 2^n-explodes -> TreeSHAP/KernelSHAP frame; added full husky example (lime_husky_full.png = LMU lime-wolfhusky3, photo+explanation per pair, snow highlighted).",
  "ml/05_interpretability deck 1 (01_linear_and_trees): corrected STALE split-impurity numbers (child 2.32M, reduction 1.43M / 38%; recomputed on bike data, not just the mis-rounding the reviewer saw); yr encoding gloss (0=2011,1=2012); enlarged RuleFit scorecard labels.",
  "Student-review workflow ran 3x (Sonnet reads rendered PNGs as first-time student): deck 2 solo, then decks 1+3 in parallel. Every valid finding verified-against-source and applied; SHAP dependence recoloured by yr; LIME robustness caption corrected; clever-Hans glossed.",
  "Committed all (7b485dd, 42 files, +486/-80) and pushed to main; GitHub Actions Render & Deploy succeeded (run 30056450472, 4m12s).",
  "ml/04_trees homework notebooks renamed to 21_* prefix (part of the commit).",
]

pending = [
  "LMU-embedded slides are black-and-white and sit inside our blue house style - a deliberate, user-approved style shift; revisit if a native restyle is ever wanted.",
]

next = [
  "Interpretability + trees[18-20] decks are authored but NOT yet delivered/recorded -> no YouTube links yet; add when recorded.",
  "Optional: broaden the LMU-embed approach to other thin spots, or restyle embeds natively.",
]

files = [
  "ml/05_interpretability/01_linear_and_trees.tex",
  "ml/05_interpretability/02_pfi_and_effects.tex",
  "ml/05_interpretability/03_shap_lime.tex",
  "ml/05_interpretability/py_src/interp_pfi_effects_figs.py",
  "ml/05_interpretability/py_src/interp_shap_figs.py",
  "ml/05_interpretability/py_src/interp_rulefit_figs.py",
  "ml/05_interpretability/fig/{pdp_2d_interaction,h_statistic_bar,lime_husky_full,lmu_shapley_value,lmu_shapley_formula,lmu_pfi_worked}.*",
  "ml/04b_classic_methods/L12b_svm_and_classic_methods.tex",
  "ml/04b_classic_methods/py_src/make_classic_figures.py",
  "ml/04_trees/21_{trees_project,trees_project_solution,adult_lightgbm,adult_lightgbm_solution}.ipynb",
]

commits = ["7b485dd"]

notes = """
LMU IML source = github.com/slds-lmu/lecture_iml (CC BY 4.0). Compiled slide PDFs live in its
slides-pdf/ folder; downloaded to scratchpad, extracted pages with pypdf, embedded as full-bleed
[plain] frames with a 'Source: LMU Interpretable Machine Learning (CC BY 4.0)' strip. Pages used:
slides02-shapley-ml pp.6-7 (value function + Shapley order-definition formula), slides02-fi-pfi p.9
(fully worked PFI: two permutations -> PFI = 1/2(0.267+0.4)), and figure/lime-wolfhusky3.png (the
6-pair husky/wolf explanation grid - the snow evidence our old lime_husky.png lacked).

Embed-vs-author call this session: embedded LMU for the husky grid and the PFI worked numbers
(their assets are strictly better); authored native for the SHAP intuition (Adi/Budi/Gogi) so it
carries the running-example flavour and controlled numbers. Same chapter, opposite calls.

Student-review discipline paid off on deck 1: the reviewer only saw the on-screen arithmetic slip
(3.75-2.28 != 1.46); recomputing on the real bike data showed two of the three numbers were stale
(child 2.32 not 2.28; reduction 1.43 not 1.46), so the 'quick fix' the reviewer suggested would have
shipped a different wrong number. Verify against source, not just against the symptom.

Session ran 2026-07-23 evening into 2026-07-24 (commit/push/render all on 07-24). 14 commits pushed
total (this session's 7b485dd + 13 earlier local that had accumulated).
"""
