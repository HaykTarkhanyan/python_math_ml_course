\documentclass[aspectratio=169]{beamer}
\input{../preamble}

\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{armblue}\bfseries,
  commentstyle=\color{paramgreen}\itshape,
  stringstyle=\color{armred},
  showstringspaces=false,
  language=Python,
  frame=single,
  framerule=0pt,
  backgroundcolor=\color{lightbg},
  numbers=none,
  breaklines=true,
  xleftmargin=4pt,
  xrightmargin=4pt,
}

\newcommand{\callout}[2]{%
  \begin{center}\fcolorbox{#1}{#1!8}{\parbox{0.92\textwidth}{#2}}\end{center}}

\title{31: Time Series -- Machine Learning Approach}
\subtitle{Forecasting as a supervised learning problem}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

% ================================================================
% Cold open
\begin{frame}{Where we left off}
  Last time: ARIMA, SARIMA, exponential smoothing -- purpose-built statistical
  models. They are excellent, and often hard to beat on a single clean series.
  \vfill
  \callout{armblue}{\textbf{Today's idea:} we already own a toolbox of powerful
  regressors -- linear models, random forests, gradient boosting, neural nets.
  What if we could point \emph{those} at a time series?}
  \vfill
  We can. The trick is to turn ``predict the next value'' into an ordinary
  $(\Xmat, \yv)$ table. Once it's a table, every model from this course applies.
  \vfill
  \footnotesize The catch: the i.i.d. assumption is still false, so we have to be
  very careful about \emph{how} we build the table and \emph{how} we validate.
  That discipline is most of this lecture.
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

% ================================================================
\section{Forecasting as supervised learning}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Forecasting as supervised learning}}\\[4pt]
    \small Slide a window over the series and out falls a design matrix.
  \end{center}
\end{frame}

\begin{frame}{The reframing}
  Pick a target ``now'' ($y_t$) and describe it with values from its own past
  ($y_{t-1}, y_{t-2}, \dots$). Each time step becomes one training row.
  \vfill
  \begin{center}
    \includegraphics[width=0.92\textwidth]{fig/supervised_reframe.pdf}
  \end{center}
  \vfill
  \callout{paramgreen}{\footnotesize That's it -- the columns on the left are
  features, the right column is the label. Feed it to \texttt{LinearRegression},
  \texttt{RandomForest}, \texttt{HistGradientBoosting} -- whatever you like.}
\end{frame}

% ================================================================
\section{Leakage and time-aware splitting}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Leakage and time-aware splitting}}\\[4pt]
    \small The single most common way to fool yourself with time series.
  \end{center}
\end{frame}

\begin{frame}{Predict-first: what's wrong with a random split?}
  You have your $(\Xmat, \yv)$ table. Instinct says
  \texttt{train\_test\_split(X, y, shuffle=True)}.
  \vfill
  \textbf{Predict: what does a random split do to a time series?}
  \pause
  \vfill
  \begin{columns}[T]
    \column{0.5\textwidth}
    \callout{armred}{\footnotesize It puts \emph{future} rows in the training set
    and \emph{past} rows in the test set. The model peeks at the future to
    ``predict'' the past. Your test score is a fantasy.}
    \column{0.5\textwidth}
    \footnotesize Worse, the rows aren't even independent: $y_{t}$ and $y_{t-1}$
    share almost everything, so a random test point sits right next to its own
    neighbours in train. Textbook \textbf{data leakage}.
  \end{columns}
  \vfill
  \footnotesize The fix is boring and non-negotiable: \textbf{split by time}.
\end{frame}

\begin{frame}{Random split vs forward split}
  \begin{center}
    \includegraphics[width=0.82\textwidth]{fig/time_aware_split.pdf}
  \end{center}
  \vfill
  \callout{paramgreen}{\footnotesize Train on an earlier block, test on the later
  block. Same rule as lecture 30: only ever learn from the past. This also means
  \textbf{no shuffling}, and any scaler / encoder must be fit on train only.}
\end{frame}

% ================================================================
\section{Feature engineering for time}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Feature engineering for time}}\\[4pt]
    \small This is where an ML forecaster is won or lost.
  \end{center}
\end{frame}

\begin{frame}{The four families of time features}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{1. Lags} -- past values.
    \begin{itemize}\scriptsize
      \item \texttt{y.shift(1)}, \texttt{shift(7)}, \texttt{shift(12)}.
      \item Pick lags at meaningful horizons (yesterday, last week, last year).
    \end{itemize}
    \vskip3pt
    \textbf{2. Rolling / window stats} -- local summaries.
    \begin{itemize}\scriptsize
      \item \texttt{y.shift(1).rolling(7).mean()} / std / min / max.
      \item \textbf{Always shift before rolling} -- else the window includes $y_t$
        itself = leakage.
    \end{itemize}
    \column{0.5\textwidth}
    \textbf{3. Calendar / date parts} -- known in advance.
    \begin{itemize}\scriptsize
      \item month, day-of-week, hour, is\_weekend, is\_holiday.
      \item Cyclically encode: $\sin/\cos(2\pi \cdot \text{hour}/24)$.
    \end{itemize}
    \vskip3pt
    \textbf{4. Exogenous regressors} -- outside drivers.
    \begin{itemize}\scriptsize
      \item price, promotion, weather, events.
      \item The big advantage of ML over plain ARIMA.
    \end{itemize}
  \end{columns}
  \vskip4pt
  \callout{armorange}{\scriptsize Golden rule: every feature for row $t$ must be
  computable using \emph{only} information available strictly before $t$.}
\end{frame}

% ================================================================
\section{Time-aware cross-validation}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Time-aware cross-validation}}\\[4pt]
    \small K-fold, but respecting the arrow of time.
  \end{center}
\end{frame}

\begin{frame}[fragile]{TimeSeriesSplit}
  Ordinary K-fold shuffles -- forbidden here. Use forward-chaining: each fold
  trains on an expanding past and validates on the next block.
  \vskip1pt
  \begin{center}
    \includegraphics[width=0.86\textwidth]{fig/ts_cv.pdf}
  \end{center}
  \vskip1pt
  \begin{columns}[T]
    \column{0.42\textwidth}
    {\lstset{basicstyle=\ttfamily\scriptsize}\begin{lstlisting}
TimeSeriesSplit(n_splits=5,
                gap=3)
\end{lstlisting}}
    \column{0.58\textwidth}
    \scriptsize
    \textbf{\texttt{max\_train\_size}} -- fix the window instead of growing it, when
    the distant past is stale.
    \vskip2pt
    \textbf{\texttt{gap}} -- drop the rows straddling the boundary. A
    \texttt{rolling(7)} feature at the start of validation was built from the last
    training days, so the two sets \emph{overlap} even after an honest split.
  \end{columns}
\end{frame}

% ================================================================
\section{Tree models and the extrapolation trap}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Tree models and the extrapolation trap}}\\[4pt]
    \small Gradient boosting is the tabular champion -- with one sharp edge.
  \end{center}
\end{frame}

\begin{frame}{Predict-first: point gradient boosting at a trending series}
  \textbf{Gradient boosting} -- a \textbf{Gradient Boosting Machine}, \textbf{GBM} on
  the charts that follow -- wins most tabular competitions (XGBoost / LightGBM /
  \texttt{HistGradientBoosting}). We built lag $+$ calendar features. Let it forecast
  the trending series from lecture 30.
  \vfill
  \textbf{Predict: does the boosting model track the upward trend?}
  \pause
  \vfill
  \begin{center}
    \includegraphics[width=0.9\textwidth]{fig/gbm_forecast.pdf}
  \end{center}
\end{frame}

\begin{frame}{Trees cannot extrapolate -- so difference first}
  \begin{center}
    \includegraphics[width=0.68\textwidth]{fig/gbm_forecast.pdf}
  \end{center}
  \vskip1pt
  \begin{columns}[T]
    \column{0.5\textwidth}
    \scriptsize A tree predicts by \emph{averaging training labels} in a leaf, so its
    output can never exceed the largest $y$ it ever saw -- and it settles well
    \emph{below} that ceiling, at the mean of its top leaf ($\approx 200$, against a
    training max of $220$).
    \vskip3pt
    Same lesson as ARIMA's ``I''. Linear models and neural networks extrapolate fine;
    \emph{trees do not}.
    \column{0.5\textwidth}
    \callout{paramgreen}{\scriptsize \textbf{The fix:} make the \emph{target}
    stationary -- predict the difference $y_t - y_{t-1}$, then add it back. Now the
    targets live in a fixed range trees can handle: MAE $19.0 \to 6.7$.}
  \end{columns}
\end{frame}

\begin{frame}{Which features actually mattered?}
  \begin{columns}[T]
    \column{0.55\textwidth}
    \includegraphics[width=\textwidth]{fig/feature_importance.pdf}
    \column{0.45\textwidth}
    Permutation importance, scored on the \textbf{test} split (lecture 23's rule --
    on train it reports what the model \emph{leans} on, on test what actually
    \emph{buys} accuracy):
    \begin{itemize}\small
      \item \texttt{dlag\_k} $=$ lag $k$ of the \textbf{differenced} target
        $d_t=y_t-y_{t-1}$. \texttt{dlag\_12}, the seasonal one, dominates at $+0.63$.
      \item \texttt{month} and \texttt{dlag\_3} split the rest.
      \item \texttt{dlag\_1} is \textbf{negative} ($-0.03$).
    \end{itemize}
    \callout{armorange}{\scriptsize A \emph{negative} importance means shuffling the
    column \textbf{helped} -- last month's change was noise, and the model is better
    off without it.}
    \vskip2pt
    \callout{armblue}{\scriptsize Domain knowledge beats brute force: knowing the
    season is 12 tells you which lag to build.}
  \end{columns}
\end{frame}

\begin{frame}{Forecasting more than one step ahead}
  For horizon $h > 1$ you have two strategies:
  \vfill
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Recursive} -- predict $t{+}1$, feed it back in as a lag, predict
    $t{+}2$, \dots
    \begin{itemize}\scriptsize
      \item One model, reused.
      \item Errors compound down the horizon.
    \end{itemize}
    \column{0.5\textwidth}
    \textbf{Direct} -- train a separate model per horizon ($h{=}1$ model,
    $h{=}2$ model, \dots).
    \begin{itemize}\scriptsize
      \item No error feedback.
      \item More models to train and store.
    \end{itemize}
  \end{columns}
  \vfill
  \callout{armorange}{\footnotesize You rarely hand-code this. Libraries like
  \texttt{skforecast}, \texttt{mlforecast} (Nixtla), \texttt{sktime} and
  \texttt{Darts} wrap any scikit-learn regressor into a recursive/direct
  forecaster with the leakage-safe plumbing built in.}
\end{frame}

% ================================================================
\section{So which wins?}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{So which approach wins?}}\\[4pt]
    \small Classical vs ML -- the honest answer.
  \end{center}
\end{frame}

\begin{frame}{Benchmark everything against the naive baseline}
  Three contenders, one held-out block, and -- this is the part people skip --
  \textbf{one stated horizon}: 18 months, forecast from a single origin.
  {\footnotesize\ Scored with \textbf{MASE} $=$ your MAE $\div$ the seasonal-naive
  model's MAE (lecture 30), so below $1$ beats copying last year.}
  \vskip1pt
  \begin{center}
    \includegraphics[width=0.66\textwidth]{fig/model_comparison.pdf}
  \end{center}
  \vskip1pt
  \callout{paramgreen}{\footnotesize A properly specified SARIMA (MASE $0.35$) and the
  gradient booster ($0.35$) are \textbf{dead level}, both far past seasonal naive
  ($1.22$). On one short, clean series the classical model was never the underdog.}
\end{frame}

\begin{frame}{Say your horizon out loud}
  The red bar is the \emph{same trained model} as the green one. The only difference
  is what we asked it.
  \vfill
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{$h=18$ (recursive)} -- forecast 18 months from one origin, feeding each
    prediction back in as the next lag. Nothing observed after the origin. MASE
    $\mathbf{0.35}$.
    \vskip4pt
    \textbf{$h=1$} -- forecast one month, then get told the true value, then forecast
    the next. MASE $\mathbf{0.39}$.
    \column{0.5\textwidth}
    \callout{armred}{\footnotesize Neither is ``the'' accuracy of the model. They
    answer different business questions -- ``plan next year's capacity'' versus
    ``restock tomorrow'' -- and a model can be excellent at one and useless at the
    other.}
    \vskip4pt
    \callout{armorange}{\footnotesize The failure mode: quoting a 1-step number and a
    competitor's 12-step number on the same chart. It is not a lie about either
    model, and it is still meaningless.}
  \end{columns}
\end{frame}

\begin{frame}{We still owe you an interval}
  Lecture 30 was blunt: \emph{a point forecast without an interval is only half an
  answer}. SARIMA hands you one for free -- it is a probability model. A gradient
  booster hands you a single number.
  \vskip4pt
  \textbf{Route 1 -- quantile regression.} Train one booster per quantile $q$, each
  with a \textbf{pinball loss} that penalises the two directions unequally:
  \[ L_q(y,\hat y) \;=\;
     \begin{cases}
       q\,(y-\hat y)      & \text{if } y \ge \hat y \quad(\text{you under-predicted}) \\[2pt]
       (1-q)(\hat y - y)  & \text{if } y <    \hat y \quad(\text{you over-predicted})
     \end{cases} \]
  \begin{columns}[T]
    \column{0.55\textwidth}
    \footnotesize At $q=0.95$ an under-prediction costs $0.95$ per unit and an
    over-prediction only $0.05$ -- \textbf{19 times} more painful to be low. So that
    model learns to sit high. Fit $q=0.05,\,0.5,\,0.95$ and the outer two \emph{are}
    your band.
    \column{0.45\textwidth}
    \callout{armblue}{\scriptsize \texttt{LGBMRegressor(objective=} \texttt{"quantile",
    alpha=0.95)} -- one fit per quantile, so three models for one band.}
  \end{columns}
\end{frame}

\begin{frame}{Route 2 -- conformal prediction}
  Fit \emph{once}, then let the model's own past errors set the width.
  \vskip3pt
  \begin{columns}[T]
    \column{0.54\textwidth}
    \begin{enumerate}\small
      \item Fit on train.
      \item Predict on a held-out \textbf{calibration} block; record the absolute
        errors.
      \item Take their empirical quantile -- that is your $\pm$.
    \end{enumerate}
    \vskip3pt
    \footnotesize Say the calibration errors, sorted, come out as
    $4,\;7,\;9,\;12,\;22$. For $90\%$ coverage take the $90$th percentile,
    $\approx\!19$, so the band is \textbf{forecast $\pm 19$}. Honest by construction:
    that is the width $90\%$ of past errors actually fell inside.
    \column{0.46\textwidth}
    \callout{armorange}{\scriptsize Model-agnostic and distribution-free -- it wraps a
    booster, an RNN, anything. The calibration block must sit \emph{after} train and
    \emph{before} test: the arrow of time again.}
    \vskip3pt
    \callout{armblue}{\scriptsize Width is \textbf{per horizon}: a 1-step band is far
    narrower than an 18-step one, so a bare ``$\pm$'' means nothing without $h$.}
  \end{columns}
  \vskip3pt
  \footnotesize Libraries: \texttt{MAPIE}, \texttt{neuralforecast}'s conformal wrapper,
  \texttt{Darts}. Straight back to lecture 14 -- an interval whose coverage you never
  check is decoration.
\end{frame}

\begin{frame}{When to reach for ML vs classical}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Classical (ARIMA / ETS) shines}
    \begin{itemize}\small
      \item one or a few series,
      \item short history,
      \item strong, stable seasonality,
      \item you need calibrated prediction intervals cheaply.
    \end{itemize}
    \column{0.5\textwidth}
    \textbf{ML (boosting / NN) shines}
    \begin{itemize}\small
      \item many related series (retail: 10k products),
      \item long history,
      \item rich exogenous features (price, weather, promos),
      \item non-linear interactions between drivers.
    \end{itemize}
  \end{columns}
  \vskip4pt
  {\footnotesize The right-hand column is really \emph{one} idea with a name: a
  \textbf{global} model -- one model fitted across many series, sharing what they have
  in common -- against SARIMA's \textbf{local} model, one fitted per series. Watch for
  it on the next few slides.}
  \vfill
  \callout{armblue}{\footnotesize In the \textbf{M5} competition (Walmart sales,
  thousands of related series), gradient-boosted trees on engineered features won.
  On the earlier \textbf{M3}/\textbf{M4} single-series benchmarks, statistical methods
  were extremely strong. Match the tool to the shape of the problem.}
\end{frame}

\begin{frame}{But the competitions gave a better answer than ``it depends''}
  \begin{columns}[T]
    \column{0.52\textwidth}
    \textbf{M4 (2018), 100{,}000 series.}
    \begin{itemize}\small
      \item \textbf{1st -- Slawek Smyl (Uber): ES-RNN.} Exponential smoothing for
        level and season, its parameters learned \emph{inside} a recurrent neural
        network. Not statistics \emph{or} ML -- one model, both.
      \item \textbf{2nd -- FFORMA} (Montero-Manso, Athanasopoulos, \textbf{Hyndman},
        Talagala). Gradient boosting, but it does not forecast: it looks at features
        of each series and predicts \emph{how to weight} a pool of classical models.
    \end{itemize}
    \column{0.48\textwidth}
    \callout{paramgreen}{\footnotesize Neither winner picked a side. One put a
    statistical model inside a neural network; the other used ML to \emph{referee}
    statistical models.}
    \vskip4pt
    \footnotesize And the runner-up's third author wrote the textbook this chapter is
    built on. The classical-vs-ML framing is a teaching device, not a real dividing
    line.
    \vskip4pt
    \callout{armorange}{\scriptsize Practical version of the same idea: average your
    SARIMA and your booster. Forecast combinations are one of the most reliably
    reproduced results in the field.}
  \end{columns}
\end{frame}

% ================================================================
\section{Deep learning and foundation models}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Deep learning and foundation models}}\\[4pt]
    \small Awareness level -- what exists and when it's worth it.
  \end{center}
\end{frame}

\begin{frame}{The deep learning lineage}
  \begin{center}
    \includegraphics[width=0.80\textwidth]{fig/deep_ts_timeline.pdf}
  \end{center}
  \vskip1pt
  \begin{columns}[T]
    \column{0.5\textwidth}
    \footnotesize \textbf{Specialist deep models}
    \begin{itemize}\scriptsize
      \item \textbf{DeepAR} -- a \textbf{recurrent} network (it carries a hidden state
        along the sequence), trained across many series, predicting a
        \emph{distribution} not a point.
      \item \textbf{N-BEATS / N-HiTS} -- deep stacks of plain layers.
      \item \textbf{Temporal Fusion Transformer} (\textbf{TFT}) -- attention $+$
        built-in importances.
      \item \textbf{PatchTST} -- patch the series, feed a Transformer.
    \end{itemize}
    \column{0.5\textwidth}
    \footnotesize \textbf{Why they connect to us}
    \vskip2pt
    \scriptsize Recurrence and attention are the subject of two later chapters, so
    treat these as a map, not a syllabus. What matters today: they need a \emph{lot} of
    data -- many series, long history -- to beat a well-tuned gradient booster. On a
    single short series like ours, they usually don't.
  \end{columns}
\end{frame}

\begin{frame}{Foundation models: zero-shot forecasting}
  Since 2023: pre-train one Transformer on billions of time points, then forecast a
  \emph{new} series with no training at all -- the same trick a
  \textbf{Large Language Model} (\textbf{LLM}) does when it answers a question it was
  never specifically trained on.
  \vfill
  \begin{columns}[T]
    \column{0.52\textwidth}
    \footnotesize \textbf{The current crop (mid-2026)}
    \begin{itemize}\scriptsize
      \item \textbf{TimesFM} (Google) -- patched decoder, pre-trained on $\sim$100B points.
      \item \textbf{Chronos-2} (Amazon) -- tokenizes values like language; five sizes
        from 9M to 710M parameters; handles covariates.
      \item \textbf{Moirai-2} (Salesforce) -- multivariate, any frequency.
      \item \textbf{TimeGPT} (Nixtla), \textbf{Sundial}, \textbf{Time-MoE},
        \textbf{Lag-Llama}.
    \end{itemize}
    \vskip2pt
    \scriptsize Don't take a vendor's word for it -- compare on a public leaderboard
    (\textbf{GIFT-Eval}, \textbf{ProbTS}) \emph{and} on your own series.
    \column{0.48\textwidth}
    \callout{armorange}{\scriptsize \textbf{Reality check.} Zero-shot is astonishing
    for a cold start with no history. But a gradient booster with good features and
    exogenous drivers is still a very strong, cheap, interpretable baseline -- and
    today it beat a tuned SARIMA by nothing at all. Try the simple thing first.}
    \vskip4pt
    \callout{armred}{\scriptsize This slide dates fastest of anything in the chapter.
    Check the names before re-lecturing.}
  \end{columns}
\end{frame}

% ================================================================
\section{Recap}

\begin{frame}{Recap -- ML for time series}
  \begin{columns}[T]
    \column{0.52\textwidth}
    \begin{itemize}\small
      \item Forecasting $\to$ supervised learning by sliding a window.
      \item \textbf{Never} random-split; split and cross-validate by time
        (\texttt{TimeSeriesSplit}, with a \texttt{gap} if your features roll).
      \item Features: lags, (shifted) rolling stats, calendar, exogenous.
      \item Trees can't extrapolate -- difference the target first.
      \item Multi-step: recursive vs direct (let a library handle it).
      \item Report MASE vs an \emph{honest} baseline, and \textbf{state the horizon}.
      \item Point forecast $\ne$ answer: quantile or conformal intervals.
      \item The M4 winners were \textbf{hybrids}, not one side or the other.
    \end{itemize}
    \column{0.48\textwidth}
    \callout{paramgreen}{\textbf{The one habit to keep:}
    \vskip3pt
    every feature and every split must respect the arrow of time. If a value
    wasn't knowable yet, it can't be in the row.}
    \vskip6pt
    \footnotesize \textbf{Toolbox:} \texttt{sktime}, \texttt{statsforecast} \&
    \texttt{mlforecast} (Nixtla), \texttt{skforecast}, \texttt{Darts},
    \texttt{Prophet}, \texttt{GluonTS}.
  \end{columns}
\end{frame}

\begin{frame}[plain]
  \begin{center}
    {\Large\color{popblue}\textbf{Questions?}}\\[6pt]
    \small Homework: build a forecaster the leakage-safe way. See the chapter page.
  \end{center}
\end{frame}

\end{document}

% ================================================================
% Provenance:
%   Source material -- Hyndman & Athanasopoulos "Forecasting: Principles and
%     Practice" (ML/CV chapters); scikit-learn TimeSeriesSplit docs; Nixtla
%     mlforecast / statsforecast docs; M4/M5 competition write-ups (M5: LightGBM
%     on engineered features won). Foundation-model facts (TimesFM, Chronos-2,
%     Moirai-2, TimeGPT, Time-MoE, zero-shot forecasting) from a 2026 web search
%     -- MachineLearningMastery "2026 Time Series Toolkit" + TSFM benchmarking
%     survey (arXiv 2510.13654). Verify names/vendors before re-lecturing as the
%     field moves fast.
%   Figures -- all Python-generated by py_src/ml_figs.py (seed 509) on the same
%     synthetic monthly series as lecture 30: supervised_reframe, time_aware_split,
%     ts_cv, gbm_forecast (extrapolation pitfall + differenced fix),
%     feature_importance, model_comparison, deep_ts_timeline. No external images.
%   Pedagogical decisions -- two predict-first frames (random split is leakage;
%     GBM flat-lines on a trend), both counter-intuitive per SLIDE_STYLE. The GBM
%     is shown LOSING at raw levels then FIXED by differencing -- honest numbers
%     (MAE 19.0 -> 6.7) rather than a staged ML victory. "When ML vs classical
%     wins" framed via M3/M4 vs M5 so students don't over-generalize from one toy
%     series. Foundation models kept awareness-level with a "try the simple thing
%     first" reality check.
%
%   Review pass 2026-07-31 -- three methodology defects found by re-running the
%     figure script; all numbers below are measured, none carried over:
%     - Renumbered 08 -> 31 (the old number collided with the delivered
%       [08] Hyperparameter tuning).
%     - The seasonal-naive baseline used series.shift(12) across the test block,
%       so 6 of its 18 values were read from INSIDE the test window. Replaced with
%       a forecast-from-origin baseline. MASE moves 0.90 -> 1.22, i.e. the old
%       "benchmark" was beating the real one by cheating. Lecture 30 now has a
%       frame on exactly this.
%     - Permutation importance was scored on TRAIN, contradicting lecture 23.
%       Moved to test; the ranking changes (month 0.195 overtakes dlag_3 0.180)
%       and dlag_1 goes NEGATIVE (-0.034), which is now its own teaching point.
%     - The GBM was scored 1-step-ahead with true lags while SARIMA forecast 18
%       steps, and no frame stated a horizon. Added a genuine recursive 18-step
%       forecast; the comparison chart is now h=18 throughout with the h=1 bar
%       fenced off behind a divider, and a frame ("Say your horizon out loud")
%       built on the contrast. Note the recursive version scores BETTER (0.35 vs
%       0.39) -- the old setup was not flattering the GBM, it was just comparing
%       two different questions.
%     - SARIMA orders changed to the airline model (0,1,1)(0,1,1)_12, matching
%       lecture 30's new correlogram. SARIMA now TIES the booster (MAE 6.07 vs
%       6.12, MASE 0.35 both), which is a truer message than the old GBM win.
%     - Added: TimeSeriesSplit gap / max_train_size (ts_cv.pdf is now three
%       panels); prediction intervals for ML (quantile + conformal), closing a
%       promise lecture 30 makes and this deck used to drop; an M4 frame on the
%       hybrid winners (Smyl's ES-RNN 1st, FFORMA 2nd -- Hyndman a co-author).
%     - Expanded on first use: GBM, TFT, LLM. RNN/MLP were forward references to
%       later chapters and are now described in words instead of acronyms.
%     - gbm_forecast.pdf is repeated on the "trees cannot extrapolate" frame; the
%       text says "left panel"/"right panel" and the figure used to be one slide
%       back, off screen.
%   Foundation-model names re-verified by web search 2026-07-31 (TimesFM,
%     Chronos-2, Moirai-2, TimeGPT, Sundial, Time-MoE; GIFT-Eval / ProbTS as the
%     leaderboards). This slide ages fastest -- recheck before re-lecturing.
%   Back pointer -- 30_classical_time_series.tex (handed off at time-aware
%     validation). Forward -- ties into the RNN (ch7) and attention chapters.
