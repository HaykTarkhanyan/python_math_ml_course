\documentclass[aspectratio=169]{beamer}
\input{../preamble}

\title{Features}
\subtitle{And what interpretability is actually for}
\author{}
\date{}

\newcommand{\keybox}[1]{%
  \begin{tcolorbox}[colback=armblue!8, colframe=armblue, boxrule=0.6pt, arc=2pt]
  #1
  \end{tcolorbox}}
\newcommand{\trapbox}[1]{%
  \begin{tcolorbox}[colback=armred!8, colframe=armred, boxrule=0.6pt, arc=2pt]
  #1
  \end{tcolorbox}}
\newcommand{\takebox}[1]{%
  \begin{tcolorbox}[colback=paramgreen!8, colframe=paramgreen, boxrule=0.6pt, arc=2pt]
  #1
  \end{tcolorbox}}
\newcommand{\watchbox}[1]{%
  \begin{tcolorbox}[colback=armorange!12, colframe=armorange, boxrule=0.6pt, arc=2pt]
  #1
  \end{tcolorbox}}

\newcommand{\promptbox}[1]{%
  \begin{center}
  \begin{tcolorbox}[colback=lightbg, colframe=gray, boxrule=0.5pt, arc=2pt, width=0.88\textwidth]
    \centering\textit{#1}
  \end{tcolorbox}
  \end{center}}

\newcommand{\demobox}[1]{%
  \begin{center}
  \begin{tcolorbox}[colback=violet1!8, colframe=violet1, boxrule=0.7pt, arc=2pt,
                    width=0.9\textwidth]
    \centering\textbf{Live:} #1
  \end{tcolorbox}
  \end{center}}

\newcommand{\sectiontransition}[2]{%
  \begin{frame}[plain]
    \vfill
    \begin{center}
      {\Huge\bfseries\color{popblue} #1}\\[1.1em]
      {\large #2}
    \end{center}
    \vfill
  \end{frame}}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

% ============================================================ COLD OPEN
\begin{frame}{The question we ended on}
  \vspace{0.7em}
  Two lectures of methods, all of them operating on \textbf{heads}, \textbf{layers} and
  \textbf{neurons} - the units the architecture happens to give us.

  \vspace{0.7em}
  And the results kept being strange: a head that suppresses the right answer, four heads waiting
  to take over, a head that stares at the answer and writes nothing.

  \vspace{1.0em}
  \begin{center}
    {\large What if heads and neurons are simply the \textbf{wrong units}?}
  \end{center}

  \vspace{1.0em}
  \keybox{Compare: you can describe a chemical reaction atom by atom, and you will be correct and
  learn nothing. \textbf{Molecules} are the level at which chemistry becomes sayable.}
\end{frame}

\begin{frame}{Two neurons}
  \vspace{0.4em}
  \begin{center}
    \includegraphics[width=0.95\textwidth]{fig/polysemantic_neuron.pdf}
  \end{center}
  \vspace{-0.3em}
  {\footnotesize Top activations over 66 sentences spanning ten unrelated domains.
  \textbf{Left:} numbers, every time, whatever the topic. \textbf{Right:} the Silk Road,
  simmering a stew, Roman emperors, freezing weather.}
\end{frame}

\begin{frame}{One of those is the exception}
  \vspace{0.7em}
  Neuron 1912 has \textbf{one readable job} - number words, in a finance sentence, a programming
  sentence and a history sentence alike. You could name it.

  \vspace{0.5em}
  Neuron 152 has \textbf{several}, unrelated. You cannot name it, because there is no single
  thing to name.

  \vspace{0.8em}
  \keybox{\textbf{Polysemanticity.} One neuron, many unrelated meanings - and in a real network
  this is the \textbf{normal case}. ``Find the neuron for X'' is usually the wrong question, and
  every method from the last two lectures inherits the problem.}

  \vspace{0.6em}
  {\footnotesize Remember lecture 1: top-activating examples are exactly the method that told a
  clean, convincing, \emph{wrong} story about a BERT neuron. This is weak evidence on purpose -
  it is the evidence these units can give.}
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

% ============================================================ SECTION 1
\section{Superposition}
\sectiontransition{Superposition}{Why the network does this to us on purpose}

\begin{frame}{Predict first}
  \vspace{1.0em}
  GPT-2 small's residual stream is \textbf{768 numbers} wide.

  \vspace{1.3em}
  \begin{center}
    {\large How many distinct concepts can it represent?}
  \end{center}

  \vspace{1.3em}
  \begin{center}
  \small
  \begin{tabular}{@{}ccc@{}}
    fewer than 768 & exactly 768 & many more than 768\\
  \end{tabular}
  \end{center}

  \vspace{1.2em}
  \begin{center}
    {\footnotesize The intuitive answer is 768 - one concept per dimension, like columns in a
    dataframe.}
  \end{center}
\end{frame}

\begin{frame}{The intuition is wrong, and the geometry says why}
  \vspace{0.3em}
  \begin{center}
    \includegraphics[width=0.90\textwidth]{fig/superposition_geometry.pdf}
  \end{center}
  \vspace{-0.4em}
  {\footnotesize \textbf{Left:} in two dimensions two random directions are usually far from
  perpendicular; by 768 the distribution collapses onto zero. \textbf{Right:} pack in more
  directions and interference grows - but slowly.}
\end{frame}

\begin{frame}{The numbers behind the right panel}
  \vspace{0.7em}
  \begin{center}
  \small
  \begin{tabular}{@{}lrrl@{}}
    \toprule
    & \textbf{4 directions} & \textbf{4096 directions} & \\
    \midrule
    $d = 16$ & 0.42 & 0.91 & \textcolor{armred}{unusable}\\
    $d = 128$ & 0.23 & 0.46 & workable\\
    $d = 768$ & \textbf{0.06} & \textbf{0.20} & \textcolor{paramgreen}{barely noticed}\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.7em}
  Read the bottom row across: multiply the number of stored directions by \textbf{a thousand} and
  the worst interference goes from $0.06$ to $0.20$.

  \vspace{0.7em}
  \keybox{At width 768 you can hold \textbf{4096} directions - more than five times the number of
  dimensions - with no pair overlapping by more than \textbf{0.20}. Nearly-orthogonal is cheap.
  \textbf{Exactly}-orthogonal is what costs a dimension.}
\end{frame}

\begin{frame}{So the network takes the deal}
  \vspace{0.7em}
  Two facts about the concepts a language model needs:

  \vspace{0.5em}
  \begin{itemize}
    \item there are \textbf{far more} of them than it has dimensions;
    \item almost all of them are \textbf{off} at any given moment - a sentence about cheese does
      not need the feature for Armenian grammar or for Python syntax.
  \end{itemize}

  \vspace{0.6em}
  \keybox{\small \textbf{Superposition.} Given sparse features and limited width, the network
  stores more features than it has dimensions, accepting a little interference in exchange for a
  lot of capacity. Polysemantic neurons are the \textbf{symptom}; this trade is the cause.}

  \vspace{0.4em}
  \takebox{\small The model is not being difficult. It is being efficient, and our preferred unit
  of analysis is the casualty.}

  \vspace{0.2em}
  {\footnotesize Elhage et al.\ (2022), \emph{Toy Models of Superposition}.}
\end{frame}

\begin{frame}{The assumption underneath all of this}
  \vspace{0.9em}
  Everything we are about to do rests on one idea:

  \vspace{0.7em}
  \begin{center}
  \begin{tcolorbox}[colback=armblue!8, colframe=armblue, boxrule=0.8pt, arc=3pt,
                    width=0.88\textwidth]
    \centering\large
    \textbf{The linear representation hypothesis}\\[0.5em]
    Concepts are stored as \textbf{directions}, and combinations of concepts are
    \textbf{sums} of those directions.
  \end{tcolorbox}
  \end{center}

  \vspace{0.9em}
  It has good evidence: steering by adding a vector works, probes find things, the whole
  attribution arithmetic from lecture 1 relies on it.

  \vspace{0.8em}
  \watchbox{It is a \textbf{hypothesis}, not a theorem. There is active work arguing that
  important structure is not linear at all. When you read that a feature ``is'' a direction, the
  word doing the work is an assumption.}
\end{frame}

% ============================================================ SECTION 2
\section{Sparse autoencoders}
\sectiontransition{Sparse autoencoders}{You have already built one of these}

\begin{frame}{Back to the autoencoder chapter}
  \vspace{0.7em}
  In chapter 8 you built a sparse autoencoder and ran it on a small recurrent network's hidden
  state. You already know:

  \vspace{0.5em}
  \begin{itemize}
    \item encoder $\rightarrow$ a \textbf{wide, sparse} code $\rightarrow$ decoder;
    \item the decoder columns form a \textbf{dictionary} of directions;
    \item the sparsity penalty is L1, the same one from Lasso in chapter 1;
    \item $L_0$ - how many features fire at once - is the number that matters.
  \end{itemize}

  \vspace{0.9em}
  \keybox{Nothing about the method is new today. What is new is the \textbf{scale}, and the fact
  that you will \textbf{use} one rather than train it.}
\end{frame}

\begin{frame}{What it does, and that it works}
  \vspace{-0.2em}
  \begin{center}
    \includegraphics[width=0.99\textwidth]{fig/sae_mechanism.pdf}
  \end{center}
  \vspace{-0.5em}
  {\footnotesize Right: we planted 8 features in 4 dimensions - guaranteed superposition, since
  there are more features than axes - and trained a small dictionary on it. Every planted feature
  came back out.}
\end{frame}

\begin{frame}{Why sparsity is the whole trick}
  \vspace{0.6em}
  Nothing in that training told the dictionary what a feature \emph{is}. There are no labels. The
  only pressure is: \textbf{rebuild the input, and keep almost everything off}.

  \vspace{0.9em}
  \keybox{If a direction has to explain many inputs at once it stays dense, and the penalty
  punishes it. The cheapest way to satisfy both terms is to give each \emph{recurring, separable}
  thing its own direction. Sparsity is what turns ``compress this'' into ``find the parts''.}

  \vspace{0.9em}
  \watchbox{Which also says exactly when it fails: a concept that is always on, or one that never
  appears alone, has no reason to get its own direction. The method finds what is
  \textbf{sparse and separable} - not what is \emph{important}.}
\end{frame}

\begin{frame}{What changes at language-model scale}
  \vspace{0.6em}
  \begin{center}
  \small
  \begin{tabular}{@{}lll@{}}
    \toprule
    & \textbf{Your homework} & \textbf{A real one}\\
    \midrule
    Read from & a 32-unit hidden state & a 768- to 5000-wide residual stream\\[0.4em]
    Dictionary size & 512 features & tens of thousands to millions\\[0.4em]
    Ground truth & you generated the corpus & none whatsoever\\[0.4em]
    Who trains it & you, in a notebook & a lab, over weeks\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.8em}
  The third row is the hard one. In your homework you could check a feature's name because you
  built the data. On real text, \textbf{nobody can}.

  \vspace{0.8em}
  \takebox{Which is why the discipline from lecture 2 carries over unchanged: naming a feature is
  a guess, and \textbf{ablating it is the test}.}
\end{frame}

\begin{frame}{Worked: how wide does the dictionary have to be?}
  \vspace{0.5em}
  Suppose a model tracks \textbf{10{,}000} concepts and typically has \textbf{20} of them active
  at once. The residual stream is \textbf{768} wide.

  \vspace{0.6em}
  \begin{center}
  \small
  \begin{tabular}{@{}lrl@{}}
    \toprule
    concepts to separate & $10{,}000$ & we want one dictionary column each\\
    dimensions available & $768$ & so $\sim$13 concepts share every dimension\\
    active at once & $20$ & $L_0 = 20$ out of $10{,}000$\\
    \midrule
    \textbf{sparsity} & $\mathbf{0.2\%}$ & of the dictionary fires on any one token\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.7em}
  \keybox{The dictionary is \textbf{13 times wider} than the space it reads from, and almost all
  of it is off at any moment. That is the whole design: \textbf{overcomplete and sparse}.}

  \vspace{0.5em}
  {\footnotesize Which is why $L_0$ is the number people quote. It is the honest measure of
  whether a dictionary is actually separating concepts or just spreading them out.}
\end{frame}

\begin{frame}{You do not have to train one}
  \vspace{0.7em}
  \textbf{Gemma Scope 2} (Google DeepMind, December 2025) is an open suite of sparse autoencoders
  and transcoders covering \textbf{every layer} of every Gemma 3 model, from 270 million to 27
  billion parameters, pretrained and instruction-tuned.

  \vspace{0.6em}
  Producing it took roughly \textbf{110 petabytes} of stored activations and over a trillion
  trained parameters.

  \vspace{0.9em}
  \keybox{That work is done, and it is free. The interesting question stopped being ``can we
  train a dictionary'' and became \textbf{``what do we ask it?''}}

  \vspace{0.8em}
  \textbf{Neuronpedia} hosts the results: a browsable index of features with their top
  activations, their effects on the output, and their connections. No installation, no GPU, no
  download.
\end{frame}

\begin{frame}{Let us actually look}
  \vspace{1.0em}
  \demobox{\texttt{neuronpedia.org} - pick a model, search for a concept, read the feature.}

  \vspace{1.0em}
  What to look at while we browse:

  \vspace{0.5em}
  \begin{itemize}
    \item the \textbf{top activating} text for a feature - is it one thing?
    \item what the feature \textbf{does to the output} when it fires;
    \item how many features look like near-duplicates of each other.
  \end{itemize}

  \vspace{0.9em}
  \watchbox{Keep lecture 1 in mind while it looks impressive. We are reading top-activating
  examples again - the method that has already fooled this field once.}
\end{frame}

\begin{frame}{Where sparse autoencoders are weak}
  \vspace{0.5em}
  \begin{itemize}
    \item \textbf{Feature splitting.} Widen the dictionary and one broad feature becomes several
      narrow ones. So the ``true'' number of features is not well defined - it is a function of
      how big a dictionary you trained.
    \item \textbf{Dead features.} A large fraction never fire at all, wasting the dictionary.
    \item \textbf{No ground truth.} We cannot check the answers, only their plausibility.
    \item \textbf{They describe representation, not computation.} A list of what is present says
      nothing about what is \emph{done} with it - the lecture-1 problem, one level up.
  \end{itemize}

  \vspace{0.5em}
  \trapbox{\small A 2025 paper states it flatly in the title: \emph{Sparse Autoencoders Do Not
  Find Canonical Units of Analysis}.}

  \vspace{0.4em}
  \takebox{\small Current advice from the field, roughly: \textbf{use them as a tool inside a
  project about something else}, not as the project.}
\end{frame}

% ============================================================ SECTION 3
\section{Attribution graphs}
\sectiontransition{Attribution graphs}{From what is represented to what is computed}

\begin{frame}{The gap that needs closing}
  \vspace{0.8em}
  \begin{center}
  \small
  \begin{tabular}{@{}ll@{}}
    \toprule
    Lecture 2 gave us & circuits, by hand, over months, for one task\\
    Sparse autoencoders give us & good units, and no circuit\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{1.0em}
  \keybox{What we want is lecture 2's \textbf{circuit}, drawn automatically, in lecture 3's
  \textbf{units}.}

  \vspace{1.0em}
  The obstacle is the multi-layer perceptron blocks: a sparse autoencoder describes the
  activations going in and coming out, but not the map between them.
\end{frame}

\begin{frame}{Transcoders}
  \vspace{0.9em}
  A \textbf{transcoder} replaces an entire block with a sparse, interpretable approximation of
  \emph{what it computes} - input features in, output features out.

  \vspace{0.9em}
  \begin{center}
  \begin{tikzpicture}[font=\small]
    \node[draw, rounded corners, fill=lightbg, minimum width=2.6cm, minimum height=0.9cm,
          align=center] (in) at (0,0) {features\\in};
    \node[draw, rounded corners, fill=armorange!22, minimum width=2.9cm, minimum height=0.9cm,
          align=center] (t) at (4.3,0) {\textbf{transcoder}\\\scriptsize sparse, readable};
    \node[draw, rounded corners, fill=paramgreen!15, minimum width=2.6cm, minimum height=0.9cm,
          align=center] (out) at (8.6,0) {features\\out};
    \draw[-{Latex[length=2.5mm]}, thick] (in) -- (t);
    \draw[-{Latex[length=2.5mm]}, thick] (t) -- (out);
  \end{tikzpicture}
  \end{center}

  \vspace{0.9em}
  \takebox{A sparse autoencoder makes the \textbf{state} readable. A transcoder makes the
  \textbf{step} readable. Chain the steps and you have a graph.}
\end{frame}

\begin{frame}{Attribution graphs}
  \vspace{-0.1em}
  Trace which features cause which, all the way to the output, and you get a wiring diagram for
  one specific prompt:

  \vspace{-0.2em}
  \begin{center}
    \includegraphics[width=0.74\textwidth]{fig/attribution_graph.pdf}
  \end{center}

  \vspace{-0.6em}
  \keybox{\small This is lecture 2's project - done automatically, in minutes, in units that mean
  something. The single biggest practical change in this field in the last two years.}
\end{frame}

\begin{frame}{What that diagram is, and is not}
  \vspace{0.7em}
  \begin{itemize}
    \item \textbf{Every arrow is a causal claim}, tested the way lecture 2 tested them: suppress
      a node and watch whether the ones downstream move.
    \item \textbf{It is specific to one prompt.} Change the wording and you get a different
      graph. This is not a diagram of the model.
    \item \textbf{Real graphs are not this tidy} - hundreds of nodes, and the readable version is
      a pruned view of a much larger object.
  \end{itemize}

  \vspace{0.8em}
  \takebox{You can generate one today without training anything: \texttt{circuit-tracer} is open
  source (Gemma-2-2B, Llama-3.2-1B, Qwen3-4B) with a browser front end on Neuronpedia.}

  \vspace{0.5em}
  {\footnotesize After Ameisen, Lindsey, Pearce et al.\ (2025), \emph{Circuit Tracing}.}
\end{frame}

\begin{frame}{Before the findings: what kind of evidence is this?}
  \vspace{0.8em}
  The next two slides report results from that work. Since this lecture ends by asking you to
  demand exactly this, it is worth stating up front:

  \vspace{0.9em}
  \begin{center}
  \small
  \begin{tabular}{@{}ll@{}}
    \toprule
    \textbf{Not} & somebody read a chain of thought and formed an impression\\[0.4em]
    \textbf{Not} & top-activating examples with a story attached\\[0.4em]
    \textbf{But} & features identified, then \textbf{suppressed and substituted}, with the
      output measured\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{1.0em}
  \takebox{In other words: the same standard as lecture 2, applied to features instead of heads.
  \textbf{They intervened.} That is why these results are worth repeating to you.}

  \vspace{0.6em}
  {\footnotesize It is still one model family, and replication elsewhere is thin. Two things can
  be true: the method is sound and the coverage is narrow.}
\end{frame}

\begin{frame}{Predict first}
  \vspace{1.0em}
  Ask a model to write a rhyming couplet. It produces the second line one token at a time, left
  to right, because that is what next-token prediction means.

  \vspace{1.3em}
  \begin{center}
    {\large When does it decide what the last word will be?}
  \end{center}

  \vspace{1.3em}
  \begin{columns}[T]
    \begin{column}{0.47\textwidth}
      \centering When it gets there - it steers into a rhyme at the end.
    \end{column}
    \begin{column}{0.47\textwidth}
      \centering Before writing any of the line.
    \end{column}
  \end{columns}

  \vspace{1.2em}
  \begin{center}
    {\footnotesize The first answer is what the training objective suggests. Commit before the
    next slide.}
  \end{center}
\end{frame}

\begin{frame}{What they found: the model plans ahead}
  \vspace{0.7em}
  Ask a model to write a rhyming couplet. The natural assumption is that it writes the second
  line word by word and arranges for a rhyme when it arrives at the end.

  \vspace{0.9em}
  \trapbox{The graph shows otherwise. The feature for the \textbf{rhyming word at the end of the
  line} is active \emph{before the model has written the words leading up to it}. It picks the
  destination, then writes a line that gets there.}

  \vspace{0.9em}
  \keybox{Next-token prediction is the \textbf{training objective}. It is not a description of
  what the model does internally. Those are different claims, and this is the clearest evidence
  we have that they come apart.}

  \vspace{0.4em}
  {\footnotesize Lindsey et al.\ (2025), \emph{On the Biology of a Large Language Model}.}
\end{frame}

\begin{frame}{And: concepts are shared across languages}
  \vspace{0.8em}
  The same features fire for a concept whether the prompt is in English, French or Chinese. The
  model appears to translate into an internal representation, operate there, and translate back
  out.

  \vspace{1.0em}
  \keybox{For a bilingual room this is worth sitting with: the evidence suggests the model is not
  running a separate Armenian model and English model. It has \textbf{one} set of concepts, with
  language as something closer to an input and output format.}

  \vspace{0.9em}
  {\footnotesize A caution: ``appears to'' is doing real work in that sentence. The finding is
  about feature overlap in one model family, not a general law about multilingual networks.}
\end{frame}

\begin{frame}{Try it yourself}
  \vspace{1.1em}
  \demobox{\texttt{neuronpedia.org/graph} - type a prompt, get its attribution graph.}

  \vspace{1.1em}
  \takebox{The thing to notice: this took a research team \textbf{months} in lecture 2, on a
  smaller model, for a simpler task. It now takes a text box and about a minute.\\[0.5em]
  That is what progress looks like in this field - not a better theory, but a tool that makes the
  old work cheap.}
\end{frame}

% ============================================================ SECTION 4
\section{Steering}
\sectiontransition{Steering}{If a concept is a direction, push in that direction}

\begin{frame}{The simplest possible intervention}
  \vspace{0.9em}
  If the linear representation hypothesis holds, then to make a model more about something, you
  add that something's direction to the residual stream:

  \vspace{0.6em}
  \[
    \text{activation} \;\leftarrow\; \text{activation} \;+\; \alpha \cdot \mathbf{v}_{\text{concept}}
  \]

  \vspace{0.9em}
  \keybox{You have seen this move before. In the diffusion chapter, classifier-free guidance
  pushed a generation toward a prompt by moving along a direction. Same idea, different object.}

  \vspace{0.8em}
  It is the cheapest control mechanism there is: no fine-tuning, no retraining, one vector
  addition at inference time.
\end{frame}

\begin{frame}{What that looks like, and what $\alpha$ has to be}
  \vspace{-0.2em}
  \begin{center}
    \includegraphics[width=0.99\textwidth]{fig/steering_geometry.pdf}
  \end{center}
  \vspace{-0.5em}
  {\footnotesize The direction is just the average residual stream on wedding sentences minus the
  average on matched neutral ones - a difference of means, nothing cleverer. At full strength the
  model's \textbf{top prediction changes from ``first'' to ``wedding''}.}
\end{frame}

\begin{frame}{The detail that catches everyone}
  \vspace{0.6em}
  $\alpha$ is meaningless on its own. It only means something \textbf{relative to the vector you
  are adding it to}.

  \vspace{0.7em}
  \trapbox{At this layer the residual stream on our prompt has norm \textbf{119}. Add a unit
  vector to it and you have changed it by under one percent - the output does not move, and it
  looks like the method failed.}

  \vspace{0.7em}
  \keybox{So steering strengths are quoted as a \textbf{multiple of the activation's own norm},
  which is how the axis on the previous slide is drawn. The same $\alpha$ at a different layer is
  a different intervention.}

  \vspace{0.5em}
  {\footnotesize It caught the first version of that experiment: $\alpha$ from 0 to 8, against a
  stream of norm 119, moved nothing - which reads like evidence that steering fails.}
\end{frame}

\begin{frame}{Golden Gate Claude}
  \vspace{0.8em}
  You met this in chapter 8. Anthropic found the feature for the Golden Gate Bridge and clamped
  it on.

  \vspace{0.7em}
  The model brought the bridge into everything - recipes, arithmetic, its own description of
  itself. Asked what it was, it said it was the bridge.

  \vspace{1.0em}
  \keybox{Why it matters as \textbf{evidence}, not as a joke: it demonstrates the feature was
  \textbf{causal}. Naming a feature from its top activations is a guess. Clamping it and watching
  identity-level behaviour follow is a test - the lecture-2 standard, applied to a feature.}
\end{frame}

\begin{frame}{The refusal direction}
  \vspace{0.8em}
  A model's willingness to refuse a request turns out to be mediated substantially by a
  \textbf{single direction} in activation space.

  \vspace{0.8em}
  \begin{columns}[T]
    \begin{column}{0.47\textwidth}
      \textbf{Add it}\\[0.3em]
      \small The model refuses more, including things it should answer.
    \end{column}
    \begin{column}{0.47\textwidth}
      \textbf{Subtract it}\\[0.3em]
      \small Safety training stops applying. This is a jailbreak, and it needs no prompt
      engineering at all.
    \end{column}
  \end{columns}

  \vspace{1.0em}
  \trapbox{The same result is a safety tool and an attack. Interpretability is not inherently
  defensive - understanding a mechanism is exactly what you need in order to disable it.}

  \vspace{0.6em}
  {\footnotesize This is a good argument for open-weight interpretability research being done in
  public, and a good argument against it. Both are real.}
\end{frame}

\begin{frame}{Steering is not editing}
  \vspace{1.0em}
  \watchbox{Steering vectors are \textbf{brittle}. The effect often does not generalise past the
  prompts it was built on, it degrades the model's general capability, and it has off-target
  consequences nobody looked for.}

  \vspace{1.0em}
  A steering vector is a demonstration that a direction is causal. It is not a reliable way to
  change what a model does in production.

  \vspace{1.0em}
  \takebox{Useful rule: treat steering as \textbf{evidence about the model}, not as a
  \textbf{feature of your product}.}
\end{frame}

% ============================================================ SECTION 5
\section{Does the model mean what it says?}
\sectiontransition{Does the model mean what it says?}{The part of this chapter you will use tomorrow}

\begin{frame}{You all use these}
  \vspace{0.8em}
  Modern reasoning models print their chain of thought - a paragraph of working before the
  answer. You read it, and it is genuinely useful.

  \vspace{1.0em}
  \begin{center}
    {\large Is that text a \textbf{transcript} of the computation,\\ or a \textbf{story about}
    the computation?}
  \end{center}

  \vspace{1.0em}
  \begin{center}
    {\footnotesize Answer honestly before the next slide. Almost everyone reads it as a
    transcript - that is what it looks like.}
  \end{center}
\end{frame}

\begin{frame}{Faithfulness, and how to test it}
  \vspace{0.7em}
  \textbf{Faithfulness} is the technical name for that assumption: does the stated reasoning
  correspond to the computation that produced the answer?

  \vspace{0.8em}
  It is testable, and the shape of the test is the one from lecture 2:

  \vspace{0.5em}
  \begin{enumerate}
    \item change something that provably affects the answer;
    \item check whether the stated reasoning mentions it.
  \end{enumerate}

  \vspace{0.9em}
  \trapbox{Where the answer moves and the explanation does not, the text is \textbf{not} a
  record of the computation. And this is measurable - it is not a philosophical worry.}
\end{frame}

\begin{frame}{Two black-box tests worth knowing}
  \vspace{0.5em}
  Neither of these needs weights, hooks, or a GPU. Both belong in the same toolkit.

  \vspace{0.7em}
  \begin{itemize}
    \item \textbf{Token forcing.} Do not ask the model to answer - \emph{begin} its answer for
      it, then let it continue. A model that refuses when asked plainly will often carry on
      happily from a first sentence it did not choose.
    \item \textbf{Prefill attacks.} The same move used adversarially: put words in the
      assistant's mouth to get past a behaviour that only ever lived in the opening tokens.
  \end{itemize}

  \vspace{0.8em}
  \keybox{What they reveal is where a behaviour is \textbf{implemented}. If a refusal survives
  having its first sentence written for it, it is doing real work. If it evaporates, it was a
  habit about how to start talking.}

  \vspace{0.6em}
  {\footnotesize Cheap, fast, and a sensible first move before reaching for anything in the last
  two lectures.}
\end{frame}

\begin{frame}{What the graphs show}
  \vspace{0.8em}
  The attribution-graph work found cases where the model's \textbf{stated reasoning does not
  match its traced computation} - the written steps and the causal path diverge.

  \vspace{1.0em}
  \keybox{We are not inferring this from behaviour. We can see one path in the graph and read a
  different one in the text.}

  \vspace{1.0em}
  Two consequences, and the second is the serious one:

  \vspace{0.5em}
  \begin{itemize}
    \item a plausible explanation is not evidence the answer is right;
    \item \textbf{reading the chain of thought is only oversight to the degree it is faithful.}
      If a model can reach an answer by a route its transcript never mentions, watching the
      transcript is not watching the model.
  \end{itemize}
\end{frame}

\begin{frame}{What this means for you, concretely}
  \vspace{0.9em}
  \takebox{The reasoning trace is \textbf{evidence about} the answer. It is not a
  \textbf{proof of} the answer.}

  \vspace{1.0em}
  Do not treat ``the model explained its steps'' as verification. That is the same category error
  we made in lecture 1, at full scale: a probe finding information is not the model using it, and
  a model narrating a method is not the model having used it.

  \vspace{0.7em}
  \watchbox{\small And the honest limit: we can detect \textbf{some} unfaithfulness. We cannot
  certify faithfulness. No procedure today takes a chain of thought and returns ``verified''.}

  \vspace{0.3em}
  {\footnotesize Interpretability for reasoning models is one of the field's most active gaps -
  the models people actually use are the ones we understand least.}
\end{frame}

% ============================================================ SECTION 6
\section{What this is for}
\sectiontransition{What this is for}{Sorting the parts you can use from the parts you watch}

\begin{frame}{Probes, in production}
  \vspace{0.8em}
  Of everything in these three lectures, this is the technique most likely to end up in your job.

  \vspace{0.7em}
  \begin{itemize}
    \item A probe is logistic regression on activations - lecture 1, chapter 3.
    \item Cheap to train, cheap to run, runs alongside the model.
    \item Already deployed for real content monitoring.
  \end{itemize}

  \vspace{0.9em}
  \keybox{If you are running an open-weight model and you need to know whether it is about to do
  something - a probe on its activations is a better detector than a classifier on its output,
  because it fires \textbf{before} the text exists.}

  \vspace{0.6em}
  \watchbox{With lecture 1's caveat attached forever: a probe tells you the information is
  \textbf{present}. Validate against behaviour before you trust it as a control.}
\end{frame}

\begin{frame}{Debugging a model you actually ship}
  \vspace{0.8em}
  The canonical example is embarrassing and real: models insisting that \textbf{9.8 is smaller
  than 9.11}.

  \vspace{0.7em}
  Not a knowledge gap - they know which is bigger if you ask differently. Interpretability work
  traced the internal features responsible, including ones that treat the strings as version
  numbers and dates.

  \vspace{0.9em}
  \takebox{The general move: \textbf{when a model fails strangely and the input looks fine, the
  explanation is in the activations.} Behavioural testing tells you \emph{that} it failed.
  Nothing else tells you \emph{why}.}
\end{frame}

\begin{frame}{Reading a model before you trust it}
  \vspace{0.8em}
  Most of you will deploy \textbf{open-weight} models - Gemma, Llama, Qwen - which are exactly
  the models the tooling in this lecture supports.

  \vspace{0.7em}
  \begin{itemize}
    \item Neuronpedia and \texttt{circuit-tracer} need no training and no GPU.
    \item Gemma Scope 2 covers every layer of every Gemma 3 size.
  \end{itemize}

  \vspace{0.9em}
  \keybox{Frame it as \textbf{due diligence}, not research. Before you put a model in front of
  customers, you can now ask what it is doing on your actual inputs - and that is a new option,
  about two years old.}
\end{frame}

\begin{frame}{Evaluating claims made at you}
  \vspace{0.9em}
  You will be told that a model ``understands'' your domain, ``reasons about'' your data, or
  ``knows'' when it is uncertain. Sometimes by a vendor, sometimes by a paper, sometimes by a
  colleague.

  \vspace{1.1em}
  \begin{center}
  \begin{tcolorbox}[colback=paramgreen!8, colframe=paramgreen, boxrule=0.8pt, arc=3pt,
                    width=0.86\textwidth]
    \centering\large
    \textbf{Did anyone intervene, or did they just look?}
  \end{tcolorbox}
  \end{center}

  \vspace{1.1em}
  \takebox{That one question separates the two halves of this chapter, and you can ask it without
  running anything. It is the most portable thing here.}
\end{frame}

\begin{frame}{What the labs are doing with it}
  \vspace{0.6em}
  Compressed deliberately - this is the part you \textbf{watch}, not the part you use.

  \vspace{0.6em}
  \begin{itemize}
    \item \textbf{Auditing games.} Hide a goal inside a model, then see whether a red team can
      find it with interpretability tools. Worth knowing about because it manufactures the
      \textbf{ground truth} this field otherwise lacks.
    \item \textbf{Model organisms.} Deliberately install a known behaviour so methods can be
      tested against a known answer. Same motivation.
    \item \textbf{Monitoring reasoning at scale}, which is section 5's problem with a budget.
  \end{itemize}

  \vspace{0.7em}
  {\footnotesize MIT Technology Review named mechanistic interpretability one of its ten
  breakthrough technologies of 2026. Context for why the field suddenly has money - \textbf{not}
  an argument that it works.}
\end{frame}

\begin{frame}{The asymmetry, said out loud}
  \vspace{1.0em}
  \trapbox{Most of the headline results in this chapter come from three or four well-funded labs,
  with model access, compute and staff that nobody in this room has.}

  \vspace{1.0em}
  That is a real limitation on the field, and you should factor it into how you read the
  literature.

  \vspace{1.0em}
  \takebox{It is \textbf{not} a reason the techniques are useless. Probes, patching, attribution
  and ablation all work fine on a 124-million-parameter model on a laptop - as every figure in
  these three lectures demonstrates. All of them were made on this machine.}
\end{frame}

% ============================================================ SECTION 7
\section{Where this leaves us}
\sectiontransition{Where this leaves us}{An honest summary, and how to go further}

\begin{frame}{What is genuinely unsolved}
  \vspace{0.7em}
  \begin{itemize}
    \item \textbf{No ground truth.} We usually cannot check whether an explanation is right,
      which is why auditing games and model organisms exist at all.
    \item \textbf{No canonical units.} Neurons are polysemantic; sparse-autoencoder features
      split as you widen the dictionary. The right unit is an open question.
    \item \textbf{Coverage.} We explain individual behaviours on individual prompts. Nobody
      explains a model.
    \item \textbf{Reasoning models} - the ones everyone uses - are the least understood.
    \item \textbf{It does not scale by hand.} Every result in these lectures took a human days.
      Getting models to do the interpreting is an active area, and nobody has made it reliable.
  \end{itemize}

  \vspace{0.6em}
  {\footnotesize A good map of the open problems: Sharkey et al.\ (2025), \emph{Open Problems in
  Mechanistic Interpretability}. The foundational read is Elhage et al.\ (2021),
  \emph{A Mathematical Framework for Transformer Circuits} - where the residual-stream picture
  from lecture 1 comes from.}
\end{frame}

\begin{frame}{The sentence to leave with}
  \vspace{1.6em}
  \begin{center}
  \begin{tcolorbox}[colback=armblue!8, colframe=armblue, boxrule=0.9pt, arc=3pt,
                    width=0.9\textwidth]
    \centering\large
    We can now say useful, testable things about what is inside a language model.\\[0.7em]
    We cannot yet explain one.
  \end{tcolorbox}
  \end{center}

  \vspace{1.6em}
  \begin{center}
    Both halves are true, and people will try to sell you either half on its own.
  \end{center}
\end{frame}

\begin{frame}{If you want to actually do this}
  \vspace{0.5em}
  \begin{center}
  \small
  \begin{tabular}{@{}ll@{}}
    \toprule
    \textbf{Start here} & \textbf{ARENA}, chapter 1 - exercises with solutions.\\
      & Sections 1.2 (the library), 1.4.1 (the circuit from lecture 2), 1.3.3 (features).\\[0.5em]
    \textbf{Zero setup} & \texttt{neuronpedia.org} - browse features and attribution graphs today.\\[0.5em]
    \textbf{Libraries} & \textbf{TransformerLens} for models up to about 9B - what these\\
      & lectures used. \textbf{nnsight} for larger models.\\[0.5em]
    \textbf{The guide} & Neel Nanda, \emph{How To Become A Mechanistic Interpretability}\\
      & \emph{Researcher} - opinionated, current, and free.\\[0.5em]
    \textbf{Programmes} & MATS, SPAR, MARS - structured mentorship, applications a few\\
      & times a year.\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.3em}
  \watchbox{\small On \emph{project} choice: we taught lecture 2's circuit because it is the
  clearest worked example, not because it is where the field is going. Nanda's guide lists
  hand-traced circuits and toy tasks like grokking among the \textbf{fading} directions.}
\end{frame}

\begin{frame}{The chapter, in one table}
  \vspace{0.4em}
  \begin{center}
  \small
  \begin{tabular}{@{}p{0.16\textwidth}p{0.36\textwidth}p{0.36\textwidth}@{}}
    \toprule
    & \textbf{What we could do} & \textbf{What it could not tell us}\\
    \midrule
    Lecture 1 & read the internals: logit lens, probes, attention, naming & whether any of it
      \emph{mattered}\\[0.5em]
    Lecture 2 & test it: ablation, patching, attribution, a full circuit & whether heads were
      the right unit\\[0.5em]
    Lecture 3 & better units: features, transcoders, attribution graphs & whether the
      explanation is \emph{correct}\\
    \bottomrule
  \end{tabular}
  \end{center}

  \vspace{0.5em}
  \keybox{\small Each lecture solved the previous one's complaint and produced a new one - an
  accurate picture of a field about eight years old.}

  \vspace{0.35em}
  \takebox{\small What survives all three: \textbf{a claim about a model's internals is worth
  what its intervention is worth.}}
\end{frame}

\end{document}

% Provenance:
% - Source material: Elhage et al. (2022) Toy Models of Superposition; Bricken et al. (2023)
%   Towards Monosemanticity; Templeton et al. (2024) Scaling Monosemanticity (Golden Gate
%   Claude); Lieberum et al. (2024) Gemma Scope + Gemma Scope 2 (DeepMind, Dec 2025, 270M-27B,
%   ~110 PB activations - web-verified 2026-08-13); Ameisen, Lindsey, Pearce et al. (2025)
%   Circuit Tracing (open-sourced May 2025; Gemma-2-2B, Llama-3.2-1B, Qwen3-4B); Lindsey et al.
%   (2025) On the Biology of a Large Language Model (rhyme planning, multilingual features,
%   stated-vs-traced reasoning); "Sparse Autoencoders Do Not Find Canonical Units of Analysis"
%   (ICLR 2025); Sharkey et al. (2025) Open Problems in Mechanistic Interpretability
%   (arXiv 2501.16496); refusal-direction work; Nanda, How To Become A Mech Interp Researcher.
% - Figures: both generated by py_src/l47_figs.py. superposition_geometry.pdf is pure numpy;
%   polysemantic_neuron.pdf is measured on GPT-2 small over a 66-sentence corpus spanning ten
%   domains, written inline so it reproduces offline. Raw numbers in results/l47_figs.json.
% - Key pedagogical decisions:
%   (1) The neuron figure shows a CLEAN neuron beside a MIXED one (1912 = numbers across every
%       domain; 152 = Silk Road / simmering / emperors / freezing). Hunting for a single dramatic
%       polysemantic neuron on a corpus this small would be the cherry-picking L45 warned about;
%       showing both is honest and makes the better point.
%   (2) The superposition capacity panel plots MEASURED worst-case interference vs the number of
%       directions. An earlier version searched for "largest n under a tolerance" and its ladder
%       capped at 4096, making capacity appear to PLATEAU - the opposite of the truth. See the
%       comment in l47_figs.py; the wrong figure would have taught the wrong lesson.
%   (3) Section 5 (chain-of-thought faithfulness) was added after review. In the first outline it
%       was a single bullet. It is the only part of the chapter that touches something students
%       look at every day, and it closes the loop on L45's "presence is not use".
%   (4) Section 6 leads with what students can USE (probes, debugging, due diligence, evaluating
%       claims) and compresses frontier-lab work to one frame, explicitly labelled as the part
%       they watch. The first outline had only lab work, which would have made the chapter feel
%       like a tour of somebody else's job.
%   (5) The closing table admits each lecture solved the previous one's complaint and created a
%       new one. Ending on an unresolved field is the honest option and the chapter has argued
%       for it throughout.
% - Chapter complete. Remaining: mech_interp.qmd chapter page, _quarto.yml registration,
%   HW_mech_interp.ipynb, and cross-links to ml/05_interpretability (black box) and
%   ml/ch18_agents/L44 (monitoring).
