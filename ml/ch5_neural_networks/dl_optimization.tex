\documentclass[aspectratio=43]{beamer}
\input{../preamble}
% 4:3 to match the embedded LMU pages. Sixth (final) chunk of the embed-based NN chapter
% (LMU week 20): challenges -> advanced optimization -> initialization -> activations.
\usepackage{pdfpages}

\title{Optimizing Neural Networks}
\date{}

\begin{document}

% ================================================================
\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Large\color{popblue}\textbf{Optimizing Neural Networks}}\\[0.5em]
    {\large Challenges, momentum \& Adam, initialization, activations}\\[1.0em]
    \begin{minipage}{0.86\textwidth}\centering\small
      This deck embeds the LMU \emph{Introduction to Deep Learning} optimization chunks in full
      (challenges $\rightarrow$ advanced optimization $\rightarrow$ initialization $\rightarrow$
      activations), with our own practical notes sprinkled in.
    \end{minipage}\\[0.9em]
    \begin{tikzpicture}[scale=0.95]
      \foreach \r in {0.35,0.6,0.85,1.1,1.35} \draw[gray!45] (0,0) ellipse ({1.9*\r} and {0.62*\r});
      \draw[armred, very thick, ->] (-2.4,0.45) -- (-1.3,-0.4) -- (-0.6,0.35)
        -- (-0.2,-0.18) -- (0,0);
      \fill[armblue] (0,0) circle (2pt);
      \node[armblue,font=\tiny,below=1pt] at (0,-0.05){minimum};
    \end{tikzpicture}
  \end{center}
  \vfill
  {\footnotesize\color{gray} Chapter 5 -- Neural Networks}
\end{frame}

% ---- LMU chunk: Challenges in optimization (38 pp) ------------
\includepdf[pages=-]{lmu_src/01_challenges.pdf}

% --- OUR SPRINKLE (after the challenges): why it works anyway ---
\begin{frame}{The landscape is scary -- and training works anyway}
  LMU just catalogued everything that can go wrong: ill-conditioning, local minima, saddle
  points, cliffs. Yet SGD trains huge nets reliably. Why?
  \vfill
  \begin{itemize}\small
    \item \textbf{Saddles, not traps.} In high dimensions almost every flat spot is a
      \textbf{saddle} (up in some directions, down in others) -- and the \textbf{noise} in
      mini-batch SGD nudges you off it. A noiseless method would sit there forever.
    \item \textbf{Most minima are fine.} Empirically, the many local minima of a big net have
      \emph{similar}, low loss -- you rarely need \emph{the} global one.
    \item \textbf{Cliffs} $\to$ \textbf{gradient clipping} (cap the step). \textbf{Ill-conditioning}
      $\to$ \textbf{momentum / adaptive learning rates} (next section).
  \end{itemize}
  \vfill
  \fcolorbox{paramgreen}{paramgreen!8}{\parbox{0.97\linewidth}{\small
    \emph{Why} non-convex SGD works so well is still an open research question -- we take it as
    an empirical fact and reach for the tools above when a specific symptom appears.}}
\end{frame}

% ---- LMU chunk: Advanced optimization (49 pp) ----------------
\includepdf[pages=-]{lmu_src/02_advanced-optim.pdf}

% --- OUR SPRINKLE (after the optimizers): the practical cheat-sheet ---
\begin{frame}{Which optimizer? A practical cheat-sheet}
  LMU derived SGD, momentum, Nesterov, AdaGrad, RMSProp, Adam and BatchNorm. In practice the
  decision is short:
  \vfill
  \begin{itemize}\small
    \item \textbf{Start with Adam} ($\rho_1{=}0.9,\ \rho_2{=}0.999,\ \alpha{=}10^{-3}$): momentum
      $+$ a per-parameter adaptive step, and it just works on most problems out of the box.
    \item \textbf{SGD $+$ momentum} ($\varphi{=}0.9$) can generalize \emph{better} once tuned --
      common for large-scale vision.
    \item \textbf{Add a schedule} (warmup $+$ cosine decay) when squeezing the last few points.
    \item \textbf{Add BatchNorm} between layers: it smooths the loss landscape, so training is
      faster and tolerates higher learning rates.
  \end{itemize}
  \vfill
  \fcolorbox{armblue}{armblue!8}{\parbox{0.97\linewidth}{\small
    The full math (momentum, Adam's moment estimates) is here and in the course's
    \emph{optimization module}. The practical default: \textbf{Adam, then tune.}}}
\end{frame}

% ---- LMU chunk: Network initialization (11 pp) ---------------
\includepdf[pages=-]{lmu_src/03_initialization.pdf}

% ---- LMU chunk: Modern activation functions (18 pp) ----------
\includepdf[pages=-]{lmu_src/04_activations.pdf}

% --- OUR SPRINKLE (after activations): the short answer ---
\begin{frame}{Which activation? The short answer}
  LMU showed the full zoo -- sigmoid, tanh, ReLU, Leaky ReLU, PReLU, ELU, SELU. In practice:
  \vfill
  \begin{itemize}\small
    \item \textbf{Hidden layers: ReLU by default.} Its derivative is $1$ for $z>0$, so gradients
      don't vanish, and it is cheap. Reach for \textbf{Leaky ReLU / GELU / ELU} only if you hit
      \emph{dying ReLUs}.
    \item \textbf{Output layer: match the task} -- \textbf{identity} for regression,
      \textbf{sigmoid} for binary, \textbf{softmax} for multiclass.
    \item \textbf{Sigmoid/tanh in hidden layers: avoid} -- they saturate and vanish the gradient
      (the wk18 recursion, made concrete).
  \end{itemize}
  \vfill
  \fcolorbox{paramgreen}{paramgreen!8}{\parbox{0.97\linewidth}{\small
    Output activation $+$ loss are two sides of one coin (maximum likelihood): sigmoid$+$cross-entropy,
    softmax$+$cross-entropy, identity$+$MSE -- which is why the output gradient is the clean
    residual $f-y$.}}
\end{frame}

% ================================================================
% OUR ADDITION: chapter close (this is the final chunk of the NN chapter).
\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Large\color{popblue}\textbf{The neural-networks chapter, end to end}}\\[0.9em]
    \begin{minipage}{0.82\textwidth}\centering\small
      Six chunks, one machine: a neuron, stacked, trained, regularized, and optimized.
    \end{minipage}
  \end{center}
  \vfill
\end{frame}

% ---------------------------------------------------------------
\begin{frame}{The whole chapter in one arc}
  \begin{itemize}\small
    \item \textbf{A neuron} $=$ affine $+$ activation $=$ a regression you know. \textbf{Hidden
      layers} learn a feature space so the boundary can bend (representation learning).
    \item \textbf{Depth \& width} make it expressive (universal approximation, folding);
      \textbf{softmax} handles many classes.
    \item \textbf{Backprop} $=$ the chain rule backward once, reusing shared error signals -- it
      gives every gradient in a single pass.
    \item \textbf{Regularization} (weight decay, early stopping, dropout, augmentation) fights
      overfitting; \textbf{optimization} (SGD$+$momentum / Adam, schedules, init, BatchNorm)
      makes training fast and stable.
  \end{itemize}
  \vfill
  \fcolorbox{paramgreen}{paramgreen!8}{\parbox{0.97\linewidth}{\small
    Every giant model from the intro deck -- LLMs, diffusion, AlphaFold -- is this same
    machinery, scaled up and specialized. You now know how it is built and trained.}}
\end{frame}

% ---------------------------------------------------------------
\begin{frame}{Where next: architectures shaped to the data}
  The MLP treats every input feature the same. Real signals have \textbf{structure} a plain MLP
  wastes:
  \vfill
  \begin{itemize}\small
    \item \textbf{Images} have \emph{locality} and \emph{translation} structure $\to$
      \textbf{convolutional networks (CNNs)}.
    \item \textbf{Sequences} (text, audio, time series) have \emph{order} and \emph{memory} $\to$
      \textbf{recurrent nets / transformers}.
  \end{itemize}
  \vfill
  \fcolorbox{armblue}{armblue!8}{\parbox{0.97\linewidth}{\small
    Same neurons, same backprop, same optimizers -- but the \emph{architecture} is shaped to the
    data. \textbf{Next chapter: convolutional neural networks.}}}
\end{frame}

\end{document}

% =============================================================
% Provenance:
%   - Sixth / final embed-based deck (2026-07-18): LMU week 20 (optimization). Largest week.
%   - Embeds LMU Introduction to Deep Learning chunks verbatim (pdfpages), 4:3:
%       01_challenges (38pp), 02_advanced-optim (49pp), 03_initialization (11pp),
%       04_activations (18pp)
%     -> unprotected copies in ml/ch5_neural_networks/lmu_src/ (PyMuPDF-decrypted).
%   - Sprinkles are PRACTICAL SYNTHESIS (LMU's coverage is already very figure-rich):
%       * after challenges: "why non-convex SGD works anyway" (noise escapes saddles, etc.);
%       * after optimizers: "which optimizer" cheat-sheet (start with Adam);
%       * after activations: "which activation" short answer (ReLU hidden, match output to task).
%   - Chapter close: full-chapter recap + bridge to CNNs (ch6).
%   - Kept alongside house-style L14/L15; rename to a playlist-numbered name once slotted.
%   - NOTE: at ~123 pages this is the biggest chunk; could be split into challenges+advanced-optim
%     and init+activations if a shorter deck is wanted.
% =============================================================
