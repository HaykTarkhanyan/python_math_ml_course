\documentclass[aspectratio=169]{beamer}
\input{../preamble}

\usepgfplotslibrary{groupplots, fillbetween}
\usepackage{listings}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{armblue}\bfseries,
  commentstyle=\color{paramgreen}\itshape,
  stringstyle=\color{armred},
  showstringspaces=false,
  language=Python,
  frame=single,
  framerule=0pt,
  backgroundcolor=\color{lightbg},
  numbers=none,
  breaklines=true,
  xleftmargin=4pt,
  xrightmargin=4pt,
}

% --- small confusion-matrix icons (layout: TP top-left, FN top-right, FP bot-left, TN bot-right) ---
\newcommand{\cmcells}{%
  \draw (0,0) grid (2,2);
  \node at (0.5,1.5){TP}; \node at (1.5,1.5){FN};
  \node at (0.5,0.5){FP}; \node at (1.5,0.5){TN};}

\title{[12] Classification Metrics}
\subtitle{Accuracy is not enough - and what to use instead}
\date{}

\begin{document}

% ================================================================
% HOOK -- bridge from L11's cliffhanger
\begin{frame}{Picking up last lecture's cliffhanger}
  Our logistic-regression cheese detector reports \textbf{97\% accuracy}. Ship it?
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    It predicts ``good'' for \emph{every} batch. Cheese is $3\%$ bad, so being right
    $97\%$ of the time means catching \textbf{zero} bad batches. \textbf{Accuracy looked
    great and was useless.}}}
  \vfill
  This deck: the metrics that actually tell you whether a classifier is any good.
  (Turning those \emph{scores} into a \emph{decision} --- picking a threshold --- is the very
  next deck.)
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
  \begin{tikzpicture}[remember picture, overlay]
    \node[anchor=east, inner sep=0pt] (dhol)
      at ([xshift=-0.8cm, yshift=0.7cm]current page.east)
      {\includegraphics[width=0.42\textwidth]{fig/dhol_64.png}};
    \node[anchor=north, font=\small\itshape, inner sep=4pt]
      at (dhol.south)
      {\href{https://youtu.be/i6OOJVFZuw0?si=8yjTVOqS2ojAm1gH}{``64''}};
  \end{tikzpicture}
\end{frame}

% ================================================================
\section{Accuracy and its problem}

\begin{frame}{The cheese factory (our running example)}
  A factory makes 1000 cheese wheels a day; about \textbf{3\% are bad} (contaminated or
  spoiled). A bad wheel reaching customers means recalls, illness, lawsuits.
  \vfill
  \begin{columns}[T]
    \column{0.52\textwidth}
    \textbf{Positive} (rare, $\sim 3\%$): a \emph{bad} batch.\\[2pt]
    \textbf{Negative} (common, $\sim 97\%$): a \emph{clean} batch.
    \column{0.48\textwidth}
    The model outputs a \textbf{score} (risk), not a hard label --- more useful, as we'll see.
  \end{columns}
  \vfill
  \footnotesize Same factory as the logistic-regression lecture; now we ask: \emph{how do we
  know the detector is any good?}
\end{frame}

\begin{frame}{What we need from a metric}
  \begin{enumerate}
    \item A number to \textbf{compare} candidate models.
    \item A way to pick a \textbf{threshold} that turns scores into decisions.
    \item Respect for the \textbf{asymmetric cost} --- missing bad cheese $\gg$ over-inspecting clean.
  \end{enumerate}
  \vfill
  \fcolorbox{armblue}{armblue!8}{\parbox{0.97\linewidth}{\small
    \textbf{The cascade} (each one fixes the previous one's flaw):
    accuracy $\to$ precision/recall $\to$ ROC AUC $\to$ PR AUC.}}
\end{frame}

\begin{frame}{Accuracy: the obvious starting point}
  \begin{columns}[T]
    \column{0.6\textwidth}
    \[ \text{Accuracy} = \frac{\text{correct}}{\text{total}} = \frac{TP+TN}{\text{all}} \]
    \begin{itemize}\small
      \item Simple to compute, simple to explain to stakeholders.
      \item ``What fraction of batches did we classify correctly?''
      \item The default metric in many tutorials.
    \end{itemize}
    \vskip 4pt
    \footnotesize Sounds reasonable --- let's see when it stops being useful.
    \column{0.4\textwidth}
    \begin{center}
      \begin{tikzpicture}[scale=0.26]
        \foreach \x in {0,...,9}
          \foreach \y in {0,...,9}
            \fill[paramgreen!30] (\x,\y) rectangle ++(0.86,0.86);
        \foreach \p in {(1,2),(4,7),(6,1),(8,8),(2,5),(9,3),(0,9),(5,4),
                        (7,6),(3,0),(6,9),(1,7),(8,2),(4,3)}
          \fill[armred!70] \p rectangle ++(0.86,0.86);
      \end{tikzpicture}\\[3pt]
      \scriptsize 100 batches: \textcolor{paramgreen!60!black}{correct} vs
      \textcolor{armred}{wrong}.\\ accuracy $=\dfrac{\text{correct}}{\text{all}}=\dfrac{86}{100}$
    \end{center}
  \end{columns}
\end{frame}

\begin{frame}{Predict-first: where accuracy breaks}
  Your factory has 1000 daily batches: 970 clean, 30 contaminated. You ship a model that
  predicts ``clean'' for \emph{every single batch} (no real model --- just the constant).
  \vfill
  \textbf{Predict:} its accuracy? its recall on contaminated batches? would you ship it?
  \vfill
  \begin{itemize}
    \item \textbf{Accuracy: $970/1000 = 97\%$.} Looks excellent.
    \item \textbf{Recall on positives: $0\%$.} Catches none of the contaminated batches.
    \item \textbf{Ship it?} Customers get sick. Lawsuits. Recalls cost millions.
  \end{itemize}
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    \textbf{The fundamental problem with accuracy:} it weights every error equally. When
    classes are imbalanced, the rare class becomes invisible. \emph{We need a metric that
    focuses on the positive class.}}}
\end{frame}

% ================================================================
\section{Precision, recall, F1}

\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Large\color{popblue}\textbf{Precision, recall, F1}}\\[0.9em]
    \begin{minipage}{0.82\textwidth}\centering\small
      Accuracy weights every error equally, so the rare $3\%$ that matters vanishes.
      Fix: stop counting all correctness and \textbf{focus on the positive class}.
    \end{minipage}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{The confusion matrix}
  All classification metrics derive from four counts. Lay them on a $2 \times 2$:
  \vfill
  \begin{center}
    \begin{tikzpicture}[every node/.style={font=\small}]
      \node at (1.5, 2.0) {\textbf{Pred.\ Positive}};
      \node at (4.5, 2.0) {\textbf{Pred.\ Negative}};
      \node at (-1.5, 1.0) {\textbf{Actual Pos.}};
      \node at (-1.5, -0.5) {\textbf{Actual Neg.}};
      \draw[armred, thick, fill=armred!10] (0, 0.4) rectangle (3, 1.6);
      \node[armred] at (1.5, 1.1) {\textbf{TP}};
      \node[armred, font=\scriptsize] at (1.5, 0.7) {bad cheese flagged};
      \draw[armorange, thick, fill=armorange!10] (3, 0.4) rectangle (6, 1.6);
      \node[armorange!80!black] at (4.5, 1.1) {\textbf{FN}};
      \node[armorange!80!black, font=\scriptsize] at (4.5, 0.7) {bad cheese missed!};
      \draw[armorange, thick, fill=armorange!10] (0, -1.1) rectangle (3, 0.1);
      \node[armorange!80!black] at (1.5, -0.4) {\textbf{FP}};
      \node[armorange!80!black, font=\scriptsize] at (1.5, -0.8) {wasted inspection};
      \draw[paramgreen, thick, fill=paramgreen!10] (3, -1.1) rectangle (6, 0.1);
      \node[paramgreen] at (4.5, -0.4) {\textbf{TN}};
      \node[paramgreen, font=\scriptsize] at (4.5, -0.8) {clean cheese passed};
    \end{tikzpicture}
  \end{center}
  \vfill
  \footnotesize In the cheese factory the costs are \textbf{asymmetric}: an \textbf{FN}
  (contaminated cheese reaches customers $\to$ recall/illness/lawsuit) is the expensive
  mistake; an \textbf{FP} (a clean batch re-inspected) is annoying but cheap.
  \vskip 2pt
  \scriptsize \emph{Heads-up:} sklearn's \texttt{confusion\_matrix} is $[TN, FP; FN, TP]$
  (negative class first); pass \texttt{labels=[1,0]} for the positive-first layout above.
\end{frame}

\begin{frame}{The same errors statisticians call Type I \& Type II}
  \begin{columns}[T]
    \column{0.5\textwidth}
    The two mistakes are the classic errors from \textbf{hypothesis testing}
    (here $H_0 =$ ``the batch is clean''):
    \begin{itemize}\small
      \item \textbf{Type I} $=$ \textbf{FP}: reject a true $H_0$ --- flag a clean batch (false alarm).
      \item \textbf{Type II} $=$ \textbf{FN}: fail to reject a false $H_0$ --- pass a bad batch (a miss).
    \end{itemize}
    \vskip 4pt
    \footnotesize Same $2\times2$, two vocabularies. We introduced these in the
    \textbf{Hypothesis Testing} lecture (\texttt{math/25\_stat\_hypothesis\_testing.qmd}).
    \column{0.5\textwidth}
    \begin{center}
      \includegraphics[width=0.92\textwidth]{fig/error_types.jpg}\\[1pt]
      \scriptsize The classic ``pregnant?'' picture of Type I vs.\ Type II.
    \end{center}
  \end{columns}
\end{frame}

\begin{frame}{Precision and recall: of what, out of what?}
  \begin{center}
    \includegraphics[width=0.62\textwidth]{fig/precision_recall.png}
  \end{center}
  \vskip 2pt
  \footnotesize Cheese mapping: \textbf{relevant} $=$ bad batches; \textbf{selected} $=$ what we flag.
  \quad \textbf{Precision} $=\frac{TP}{TP+FP}$ --- of what we flagged, how many are bad (efficiency).
  \quad \textbf{Recall} $=\frac{TP}{TP+FN}$ --- of all bad, how many we flagged (safety).
\end{frame}

\begin{frame}{F1: when you need a single number}
  \begin{columns}[T]
    \column{0.46\textwidth}
    \textbf{F1} $=$ the \emph{harmonic} mean of precision and recall:
    \[ F_1 = 2 \cdot \frac{P \cdot R}{P + R} \]
    \textbf{Why harmonic?} It is dominated by the \emph{smaller} value:
    \begin{center}\footnotesize
      \renewcommand{\arraystretch}{1.15}
      \begin{tabular}{cccc}
        P & R & arith. & F1 \\ \hline
        1.0 & 0.0 & 0.5 & \textbf{0.0} \\
        0.8 & 0.8 & 0.8 & 0.8 \\
        0.5 & 0.9 & 0.7 & 0.64 \\
        0.9 & 0.5 & 0.7 & 0.64
      \end{tabular}
    \end{center}
    \column{0.54\textwidth}
    \begin{center}
      \includegraphics[width=\linewidth]{fig/cm_f1_heatmap.pdf}\\[-3pt]
      \scriptsize F1's contours \textbf{bow into the corners} --- one low coordinate drags it
      down. The arithmetic mean's are straight (a high P hides a low R). Dots $=$ the table rows.
    \end{center}
  \end{columns}
  \vskip 2pt
  \footnotesize Arithmetic mean lets a $1.0$ hide a $0.0$; F1 refuses --- so it can't be gamed by an
  extreme threshold. \;\; $F_\beta = (1+\beta^2)\frac{P\cdot R}{\beta^2 P + R}$: higher $\beta$
  weights recall ($\beta=2$ fits the cheese factory).
\end{frame}

\begin{frame}{Worked example: cheese batch inspection}
  50 batches inspected; 10 actually bad; the model flags 13.
  \vfill
  \begin{columns}[T]
    \column{0.46\textwidth}
    \begin{center}\small
    \begin{tabular}{lcc}
       & \textbf{Pred Bad} & \textbf{Pred Good} \\ \hline
      Actual Bad (10) & TP $=8$ & FN $=2$ \\
      Actual Good (40) & FP $=5$ & TN $=35$ \\
    \end{tabular}
    \end{center}
    \column{0.54\textwidth}
    \begin{center}\small
    \begin{tabular}{lll}
      \textbf{Metric} & \textbf{Calc.} & \textbf{Value} \\ \hline
      Accuracy & $(8{+}35)/50$ & $86\%$ \\
      Recall & $8/(8{+}2)$ & $80\%$ \\
      Precision & $8/(8{+}5)$ & $62\%$ \\
      F1 & $2{\cdot}.62{\cdot}.80/(.62{+}.80)$ & $70\%$ \\
    \end{tabular}
    \end{center}
  \end{columns}
  \vfill
  \footnotesize Accuracy ($86\%$) hides the 2 missed bad batches; recall ($80\%$) is the
  operational truth; precision ($62\%$) says $38\%$ of inspections were wasted; F1 ($70\%$)
  punishes the gap between the two.
\end{frame}

% ================================================================
\section{Threshold dependence and the ROC fix}

\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Large\color{popblue}\textbf{Thresholds and the ROC curve}}\\[0.9em]
    \begin{minipage}{0.82\textwidth}\centering\small
      Precision, recall and F1 all shift as you move the cutoff $c$. Fix: summarize a
      model across \emph{every} threshold at once.
    \end{minipage}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Precision and recall come from a threshold}
  \begin{columns}[T]
    \column{0.48\textwidth}
    ``Flag more $\to$ recall up, precision down'' --- that tension \textbf{is} the threshold
    knob. The model outputs a \emph{score}; a cutoff turns it into a label:
    \[ \text{predict positive} \iff \text{score}(\xv) > c. \]
    \begin{itemize}\small
      \item \textbf{Lower $c$}: flag more $\to$ recall up, precision down (more false alarms).
      \item \textbf{Higher $c$}: flag fewer $\to$ precision up, recall down (more misses).
      \item \textbf{$c = 0.5$ is just one cut} --- often not the right one.
    \end{itemize}
    \column{0.52\textwidth}
    \begin{center}
      \includegraphics[width=\linewidth]{fig/cm_score_dist.pdf}\\[-3pt]
      \scriptsize Slide $c$: the four counts (so P and R) all change. Illustrative, balanced.
    \end{center}
  \end{columns}
  \vskip 2pt
  \footnotesize So instead of one $(P,R)$ pair, summarize the model over \emph{all} thresholds (next).
\end{frame}

\begin{frame}{Predict-first: P and R at extreme thresholds}
  Two extreme cutoffs --- what are precision and recall at each?
  \begin{itemize}
    \item $c = 0$: predict positive for \emph{every} batch.
    \item $c = 1$: predict negative for \emph{every} batch.
  \end{itemize}
  \vfill
  \begin{itemize}\small
    \item \textbf{$c = 0$:} $FN = 0 \Rightarrow$ \textbf{recall $=100\%$}; $FP =$ all negatives $\Rightarrow$ \textbf{precision $=$ base rate} ($\sim 3\%$).
    \item \textbf{$c = 1$:} $TP = 0 \Rightarrow$ \textbf{recall $=0\%$}; precision $= 0/0$ (sklearn returns $0$).
  \end{itemize}
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    Each of P and R is \textbf{trivially gamed} by an extreme threshold. F1 stops that but
    \emph{still depends on the threshold you picked}. We want a metric that summarizes
    performance \emph{across all thresholds at once}.}}
\end{frame}

\begin{frame}{The ROC curve: TPR vs.\ FPR}
  Two ratios: \textbf{TPR} $= \text{recall} = \dfrac{TP}{TP+FN}$ \quad and \quad
  \textbf{FPR} $= \dfrac{FP}{FP+TN}$ (the false-alarm rate).
  \vfill
  \textbf{Sweep} the cutoff from $c = 1$ (nothing flagged) down to $c = 0$ (all flagged); at
  each step plot the point $(\text{FPR}, \text{TPR})$. The trail is the \textbf{ROC curve}.
  \vfill
  \begin{itemize}\small
    \item Top-left corner $=$ perfect (TPR $=1$ at FPR $=0$).
    \item Diagonal $=$ random guessing; below it $=$ worse than random (flip the labels).
  \end{itemize}
  \vfill
  \footnotesize Let's build one, point by point (next).
\end{frame}

\begin{frame}{Building a ROC curve --- the recipe}
  \begin{center}
    \includegraphics[width=0.78\textwidth]{fig/roc_build_1_crop.png}
  \end{center}
  \vskip 2pt
  \scriptsize Source: LMU \emph{Introduction to Machine Learning} (i2ml), CC BY 4.0.
\end{frame}

\begin{frame}{Step by step: high $c$ first, TPR climbs}
  \begin{columns}[c]
    \column{0.5\textwidth}\centering\includegraphics[width=\textwidth]{fig/roc_build_2_crop.png}
    \column{0.5\textwidth}\centering\includegraphics[width=\textwidth]{fig/roc_build_3_crop.png}
  \end{columns}
  \vskip 2pt
  \footnotesize As $c$ drops past the top-ranked points (all real positives here), \textbf{TPR
  steps up} while FPR stays $0$. \hfill \scriptsize Source: LMU i2ml, CC BY 4.0.
\end{frame}

\begin{frame}{Step by step: lower $c$, FPR moves right}
  \begin{columns}[c]
    \column{0.5\textwidth}\centering\includegraphics[width=\textwidth]{fig/roc_build_4_crop.png}
    \column{0.5\textwidth}\centering\includegraphics[width=\textwidth]{fig/roc_build_5_crop.png}
  \end{columns}
  \vskip 2pt
  \footnotesize Once negatives start crossing the threshold, \textbf{FPR steps right}.
  \hfill \scriptsize Source: LMU i2ml, CC BY 4.0.
\end{frame}

\begin{frame}{The finished ROC curve}
  \begin{columns}[T]
    \column{0.62\textwidth}
    \includegraphics[width=\textwidth]{fig/roc_build_6_crop.png}
    \column{0.38\textwidth}
    \small At $c = 0$ everything is flagged $\Rightarrow$ $(\text{FPR},\text{TPR}) = (1,1)$.
    \vskip 4pt
    The staircase joining $(0,0)$ to $(1,1)$ is the \textbf{ROC curve} --- one curve summarizing
    the model at \emph{every} threshold.
    \vskip 6pt
    \scriptsize Source: LMU i2ml, CC BY 4.0.
  \end{columns}
\end{frame}

\begin{frame}{ROC AUC}
  Summarize the whole ROC curve as one number: the \textbf{area under the curve}.
  \begin{columns}[T]
    \column{0.5\textwidth}
    \footnotesize
    \begin{itemize}\footnotesize
      \item AUC $= 1.0$: perfect (curve hits top-left).
      \item AUC $= 0.5$: random (curve $=$ diagonal).
      \item AUC $= 0.0$: perfectly wrong (flip labels).
    \end{itemize}
    \vskip 6pt
    \textbf{Probabilistic meaning.} AUC $=$ the probability that a random positive scores
    higher than a random negative (the Mann-Whitney $U$ statistic) --- a real probability,
    not just an area.
    \vskip 6pt
    \textbf{Threshold-free.} Compares models on ranking quality without picking a cutoff.
    \column{0.5\textwidth}
    \begin{center}
      \includegraphics[width=\textwidth,height=3.7cm,keepaspectratio]{fig/cm_roc.pdf}\\[-2pt]
      \scriptsize Real cheese model: shaded area $=$ AUC.
    \end{center}
  \end{columns}
  \vfill
  \footnotesize \textbf{Footgun:} feed AUC the \emph{scores} (\texttt{predict\_proba}), not the
  predicted labels --- labels collapse it toward accuracy.
\end{frame}

% ================================================================
\section{When ROC fails: PR and lift}

\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Large\color{popblue}\textbf{When ROC lies: PR and lift}}\\[0.9em]
    \begin{minipage}{0.82\textwidth}\centering\small
      On rare-positive data the huge negative class hides real differences in ROC.
      Fix: measure precision directly --- the PR curve.
    \end{minipage}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{Predict-first: ROC's blind spot on imbalanced data}
  Two models, same imbalanced cheese data: 100 bad batches, 10{,}000 clean.
  \vfill
  \begin{center}\small
    \begin{tabular}{lcccc}
      & \textbf{TP} & \textbf{FP} & \textbf{FN} & \textbf{TN} \\ \hline
      Model A & 91 & 10 & 9 & 9{,}990 \\
      Model B & 91 & 100 & 9 & 9{,}900 \\
    \end{tabular}
  \end{center}
  \vfill
  \textbf{Predict:} recall, FPR, precision for each --- which would you ship?
  \vfill
  \begin{center}\small
    \begin{tabular}{lccc}
      & \textbf{Recall} & \textbf{FPR} & \textbf{Precision} \\ \hline
      Model A & 91\% & $10/10{,}000 = \mathbf{0.10\%}$ & $91/101 = \mathbf{90\%}$ \\
      Model B & 91\% & $100/10{,}000 = \mathbf{1.0\%}$ & $91/191 = \mathbf{48\%}$ \\
    \end{tabular}
  \end{center}
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    \textbf{ROC's blind spot:} FPR barely moved ($0.1\% \to 1\%$) --- tiny in ROC space ---
    yet precision \emph{crashed} $90\% \to 48\%$ (half of B's inspections wasted). The huge
    negative class dominates the FPR denominator and hides the operational difference.}}
\end{frame}

\begin{frame}{The PR curve}
  Same threshold sweep, but plot \textbf{precision vs.\ recall} instead of TPR vs.\ FPR.
  \begin{columns}[T]
    \column{0.5\textwidth}
    \footnotesize
    \begin{itemize}\footnotesize
      \item Top-right $=$ perfect (precision $=1$, recall $=1$).
      \item Top-left $=$ strict threshold (few but high-quality alarms).
      \item Bottom-right $=$ loose threshold (many alarms, many false).
      \item Baseline: ``always positive'' has precision $=$ base rate.
    \end{itemize}
    \vskip 6pt
    \textbf{Doesn't depend on TN} --- the TN count never enters precision or recall. That is
    exactly what makes PR informative when positives are rare.
    \column{0.5\textwidth}
    \begin{center}
      \includegraphics[width=\textwidth]{fig/cm_pr.pdf}\\[-2pt]
      \scriptsize Real PR curve (note the zig-zag); dashed baseline $=$ base rate.
    \end{center}
  \end{columns}
\end{frame}

\begin{frame}{PR AUC (Average Precision)}
  \begin{columns}[T]
    \column{0.54\textwidth}
    Summarize the PR curve as one number: \textbf{average precision (AP)} $=$ the
    \textbf{area under the PR curve}.
    \vskip 3pt
    \textbf{Read it operationally.} Walk down the score-ranked list; each new bad batch ticks
    recall up --- record the precision \emph{there}. AP averages those precisions, weighted by
    the recall gained:
    \[ \text{AP} = \sum_n \underbrace{(R_n - R_{n-1})}_{\text{recall gained}} \cdot
       \underbrace{P_n}_{\text{precision here}}. \]
    \column{0.46\textwidth}
    \begin{center}
      \includegraphics[width=\linewidth]{fig/cm_pr_ap.pdf}\\[-2pt]
      \scriptsize Shaded $=$ AP. Compare to base rate $0.03$ (random AP $=$ base rate, \emph{not $0.5$}).
    \end{center}
  \end{columns}
  \vskip 2pt
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    \textbf{Sensitive to imbalance by design:} in the A-vs-B example ROC AUC barely moves
    ($0.99\!\to\!0.96$) yet AP \emph{crashes} ($0.72\!\to\!0.35$). PR AUC matches operational reality.}}
\end{frame}

\begin{frame}{Cumulative gain: the ``top-$k$\%'' question}
  Operational constraint: the factory can inspect only the top \textbf{20\%} most-suspicious
  batches per day. Rank by score --- how much of the problem does that catch?
  \begin{columns}[T]
    \column{0.52\textwidth}
    \textbf{Cumulative gain chart.} Rank all batches by score; $x =$ fraction inspected,
    $y =$ fraction of \emph{all} bad batches caught.
    \begin{itemize}\small
      \item \textbf{Random} $=$ diagonal (inspect $20\%$ $\to$ catch $20\%$ of the bad).
      \item \textbf{Perfect} $=$ climbs to $100\%$ by the base rate, then flat.
      \item \textbf{Our model} bows up: the top $20\%$ already catches $\sim\!64\%$ of bad batches.
    \end{itemize}
    \vskip 4pt
    \footnotesize Answers what production asks: \emph{given my budget, how much of the problem
    do I catch?}
    \column{0.48\textwidth}
    \begin{center}
      \includegraphics[width=\textwidth]{fig/cm_lift.pdf}\\[-2pt]
      \scriptsize Real cheese model; top $20\%$ catches $\sim\!64\%$ of all bad batches.
    \end{center}
  \end{columns}
\end{frame}

\begin{frame}{Lift: how much better than random?}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Lift} rescales gain by the budget:
    \[ \text{lift}(k) = \frac{\text{\% of bad caught in the top }k\%}{k\%}. \]
    \begin{itemize}\small
      \item Random $=1$; \textbf{lift $>1$} $=$ better than random for the same budget.
      \item Top $20\%$ here: $64\%/20\% = \mathbf{3.2\times}$.
    \end{itemize}
    \vskip 4pt
    \textbf{Lift per decile.} Split the ranked batches into $10$ equal groups. The
    \textbf{first decile} --- the $10\%$ the model finds most suspicious --- has $\mathbf{5\times}$
    the base bad-rate; by the $4$th decile lift is already $\le 1$.
    \vskip 3pt
    \footnotesize The steep drop \emph{is} the ranking power: the value is concentrated at the top.
    \column{0.5\textwidth}
    \begin{center}
      \includegraphics[width=\textwidth]{fig/cm_lift_decile.pdf}\\[-2pt]
      \scriptsize Bad-rate per decile $\div$ base rate. Decile $1 = 5\times$; the tail is $\approx$ random.
    \end{center}
  \end{columns}
\end{frame}

\begin{frame}{ROC vs.\ PR side-by-side on imbalanced data}
  Same imbalanced dataset (1\% positive), same two models. Two views:
  \begin{center}
    \includegraphics[width=0.8\textwidth]{fig/cm_roc_vs_pr.pdf}
  \end{center}
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    \textbf{Same models, two stories.} ROC: both near top-left (AUC $0.96$--$0.98$). PR: A
    holds precision (AP $0.72$), B collapses toward baseline (AP $0.35$). On imbalanced data
    \emph{ROC overstates and PR clarifies} --- reach for PR when the positive class is rare.}}
\end{frame}

% ================================================================
\section{Practical: choosing the metric}

\begin{frame}{The metric zoo: one matrix, many names}
  Every metric is a ratio of $TP, FP, FN, TN$ --- they split by \emph{what you condition on}:
  \vskip 4pt
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Given the actual class} (row-wise):
    \begin{itemize}\small
      \item \textbf{TPR} $=$ recall $=$ sensitivity $= \frac{TP}{TP+FN}$
      \item \textbf{FNR} $=$ miss rate $= \frac{FN}{TP+FN} = 1-\text{TPR}$
      \item \textbf{TNR} $=$ specificity $= \frac{TN}{TN+FP}$
      \item \textbf{FPR} $=$ fall-out $= \frac{FP}{FP+TN} = 1-\text{TNR}$
    \end{itemize}
    \column{0.5\textwidth}
    \textbf{Given the prediction} (column-wise):
    \begin{itemize}\small
      \item \textbf{PPV} $=$ precision $= \frac{TP}{TP+FP}$
      \item \textbf{FDR} $= \frac{FP}{TP+FP} = 1-\text{PPV}$
      \item \textbf{NPV} $= \frac{TN}{TN+FN}$
      \item \textbf{FOR} $= \frac{FN}{TN+FN} = 1-\text{NPV}$
    \end{itemize}
  \end{columns}
  \vfill
  \fcolorbox{armblue}{armblue!8}{\parbox{0.985\linewidth}{\small
    \textbf{Overall:} accuracy $=\frac{TP+TN}{\text{all}}$; \textbf{balanced accuracy}
    $=\frac{\text{TPR}+\text{TNR}}{2}$ (imbalance-robust); \textbf{F1} (harmonic mean of
    precision \& recall); \textbf{MCC} $\in[-1,1]$ (correlation of prediction \& truth, $0=$
    random). ROC plots TPR vs.\ FPR; PR plots precision vs.\ recall.}}
\end{frame}

\begin{frame}{Which metric for which problem?}
  \begin{center}\small
    \begin{tabular}{p{4.5cm} p{4.3cm} p{4cm}}
      \textbf{Situation} & \textbf{Use} & \textbf{Why} \\ \hline
      Balanced classes, single threshold & F1 (or accuracy) & Single number; symmetric. \\
      Balanced, threshold-free & ROC AUC & Compares ranking quality. \\
      \textbf{Imbalanced, threshold-free} & PR AUC (avg.\ precision) & ROC overstates; PR matches reality. \\
      Bounded inspection budget & Lift / gain at top-$k$\% & Capacity is the real constraint. \\
      FN $\gg$ FP (cheese, fraud, screening) & Recall, with a precision floor & Asymmetric cost. \\
      FP $\gg$ FN (spam, churn calls) & Precision, with a recall floor & Asymmetric cost, other way. \\
      One imbalance-robust number & MCC (Matthews CC) & Correlation of prediction \& truth; uses \emph{all four} cells, so unlike F1/accuracy it isn't fooled by the majority class. \\
    \end{tabular}
  \end{center}
  \vfill
  \scriptsize \textbf{Default:} report 3--4 metrics --- one threshold-free (ROC or PR) plus
  precision/recall at a chosen threshold. \emph{Calibration is its own lecture.}
\end{frame}

\begin{frame}[fragile]{Computing these in \texttt{sklearn}}
\begin{lstlisting}
from sklearn.metrics import (classification_report, confusion_matrix,
    roc_auc_score, average_precision_score, matthews_corrcoef)

proba  = clf.predict_proba(X_test)[:, 1]   # positive-class score
y_pred = (proba >= 0.5).astype(int)        # threshold -> labels

# threshold-free -- feed the SCORES:
roc_auc_score(y_test, proba)               # ROC AUC
average_precision_score(y_test, proba)     # PR AUC

# threshold-dependent -- feed the LABELS:
print(classification_report(y_test, y_pred))     # P/R/F1 + support
confusion_matrix(y_test, y_pred, labels=[1, 0])  # positive-first
matthews_corrcoef(y_test, y_pred)                # MCC
\end{lstlisting}
  \vskip 2pt
  \fcolorbox{armred}{armred!8}{\parbox{0.97\linewidth}{\small
    \textbf{The one footgun:} the ranking metrics (\texttt{roc\_auc\_score},
    \texttt{average\_precision\_score}) take the \emph{scores}; everything else takes
    \emph{predicted labels}. Passing labels to AUC silently collapses it toward accuracy.}}
\end{frame}

% ================================================================
\section{Multi-class evaluation}

\begin{frame}[plain]
  \vfill
  \begin{center}
    {\Large\color{popblue}\textbf{Beyond two classes}}\\[0.9em]
    \begin{minipage}{0.82\textwidth}\centering\small
      With $K$ classes the confusion matrix grows to $K\times K$, and the binary metrics need
      an averaging rule. (Cheese defects: good / moldy / dry.)
    \end{minipage}
  \end{center}
  \vfill
\end{frame}

\begin{frame}{The $K\times K$ confusion matrix}
  \begin{columns}[T]
    \column{0.52\textwidth}
    \includegraphics[width=\textwidth]{fig/cm_multiclass.pdf}
    \column{0.48\textwidth}
    \small
    \begin{itemize}\small
      \item Diagonal $=$ correct; off-diagonal shows \emph{which classes get confused}.
      \item Here \textbf{moldy} and \textbf{dry} bleed into each other ($30$ and $49$); \textbf{good} is clean.
      \item Per-class precision/recall come from \textbf{one-vs-rest}: treat ``class $k$ vs.\ the rest'' as a binary problem and read its row/column.
    \end{itemize}
    \vskip 4pt
    \footnotesize One matrix, $K$ binary views.
  \end{columns}
\end{frame}

\begin{frame}{Averaging: macro vs.\ micro vs.\ weighted}
  Compute the binary metric one-vs-rest per class, then average. Three ways:
  \vfill
  \begin{itemize}
    \item \textbf{Macro:} average per-class, \emph{equal weight per class}. Best when every class
      matters regardless of size (``how well on each cheese type?'').
    \item \textbf{Micro:} pool all $TP/FP/FN$ across classes, then compute. \emph{Equals accuracy}
      for single-label problems. Best for overall correctness.
    \item \textbf{Weighted:} per-class, weighted by class support. Reflects the data distribution
      (the common default in reports).
  \end{itemize}
  \vfill
  \footnotesize Multi-class AUC: average per-class one-vs-rest (macro-OVR is standard).
  \vskip 2pt
  \scriptsize \emph{Caveat:} with few samples in a class, its per-class F1 is noisy and macro F1
  inherits that noise --- always report \texttt{support} (samples per class).
\end{frame}

\begin{frame}{Averaging: a worked example}
  Three cheese types, \textbf{imbalanced}. Per-class recall (each computed one-vs-rest):
  \vskip 4pt
  \begin{columns}[T]
    \column{0.46\textwidth}
    \begin{center}\small
      \renewcommand{\arraystretch}{1.3}
      \begin{tabular}{l c c}
        class & support & recall \\
        \hline
        good  & $100$ & $95/100 = 0.95$ \\
        moldy & $50$  & $40/50 = 0.80$ \\
        \textbf{dry (rare)} & $10$ & $\mathbf{4/10 = 0.40}$ \\
        \hline
      \end{tabular}
    \end{center}
    \column{0.54\textwidth}
    \begin{itemize}\small
      \item \textbf{Macro} $= \dfrac{0.95+0.80+0.40}{3} = \mathbf{0.72}$ \\
        {\footnotesize (each class counts once)}
      \item \textbf{Micro} $= \dfrac{95+40+4}{160} = \mathbf{0.87}$ \\
        {\footnotesize (pool all $\Rightarrow$ this is accuracy)}
      \item \textbf{Weighted} $= \dfrac{100(.95)+50(.80)+10(.40)}{160} = \mathbf{0.87}$
    \end{itemize}
  \end{columns}
  \vfill
  \fcolorbox{armblue}{armblue!8}{\parbox{0.97\linewidth}{\small
    The rare \textbf{dry} class fails ($0.40$), but only \textbf{macro} surfaces it ($0.72$).
    Micro and weighted are dominated by the big \textbf{good} class and both report $0.87$ ---
    the same blind spot accuracy had. \textbf{Use macro when the rare classes must count.}}}
\end{frame}

\begin{frame}{Top-$k$ accuracy: is the truth in the top $k$ guesses?}
  \begin{columns}[T]
    \column{0.55\textwidth}
    With \textbf{many} classes, demanding the \emph{single} best guess be right is harsh.
    \textbf{Top-$k$ accuracy} counts a prediction correct if the true class is among the model's
    $k$ highest-probability classes.
    \begin{itemize}\small
      \item \textbf{Top-1} $=$ ordinary accuracy.
      \item \textbf{Top-5} is the classic ImageNet metric ($1000$ classes): \emph{did the right
        label make the shortlist?}
      \item Fits when classes are many or easily confused, or when a human makes the final call
        from the model's shortlist.
    \end{itemize}
    \vskip 2pt
    \footnotesize sklearn: \texttt{top\_k\_accuracy\_score(y, proba, k=5)} --- feed the scores.
    \column{0.45\textwidth}
    \begin{center}\small
    One wheel, true class \textbf{dry}. The model's ranked probabilities:
    \vskip 3pt
    \renewcommand{\arraystretch}{1.25}
    \begin{tabular}{c l c}
      rank & class & prob \\
      \hline
      1 & moldy & $0.50$ \\
      2 & \textbf{dry} & $0.30$ \\
      3 & good & $0.20$ \\
      \hline
    \end{tabular}
    \vskip 5pt
    \textcolor{armred}{\textbf{Top-1: wrong}} (guesses moldy).\\
    \textcolor{paramgreen!60!black}{\textbf{Top-2: correct}} (dry is in the top 2).
    \end{center}
  \end{columns}
\end{frame}

% ================================================================
\section{Wrap-up}

\begin{frame}{Decision flowchart}
  \begin{center}
    \begin{tikzpicture}[
      node distance=0.4cm and 0.7cm,
      box/.style={rectangle, draw=armblue, very thick, text width=3cm, align=center, font=\scriptsize, rounded corners=2pt, fill=armblue!5, minimum height=0.85cm},
      result/.style={rectangle, draw=paramgreen, very thick, text width=3cm, align=center, font=\scriptsize, rounded corners=2pt, fill=paramgreen!10, minimum height=0.85cm},
      arr/.style={->, >=stealth, thick, armblue},
    ]
      \node[box] (start) {Multi-class?};
      \node[result, right=of start] (mc) {macro/micro/weighted F1; classification\_report};
      \node[box, below=of start] (imb) {Imbalanced classes?};
      \node[result, right=of imb] (pr) {PR AUC + precision/recall at chosen threshold};
      \node[box, below=of imb] (cap) {Bounded inspection / contact budget?};
      \node[result, right=of cap] (lift) {Lift / gain at top-$k$\%};
      \node[box, below=of cap] (sym) {Symmetric cost, balanced data?};
      \node[result, right=of sym] (roc) {ROC AUC + F1};
      \node[result, below=of sym] (cost) {Accuracy + threshold tuned by cost};

      \draw[arr] (start) -- node[above, font=\tiny]{yes} (mc);
      \draw[arr] (start) -- node[left, font=\tiny]{no} (imb);
      \draw[arr] (imb) -- node[above, font=\tiny]{yes} (pr);
      \draw[arr] (imb) -- node[left, font=\tiny]{no} (cap);
      \draw[arr] (cap) -- node[above, font=\tiny]{yes} (lift);
      \draw[arr] (cap) -- node[left, font=\tiny]{no} (sym);
      \draw[arr] (sym) -- node[above, font=\tiny]{yes} (roc);
      \draw[arr] (sym) -- node[left, font=\tiny]{no} (cost);
    \end{tikzpicture}
  \end{center}
  \vfill
  \footnotesize \textbf{Always report multiple metrics.} A single number hides the trade-off; the trade-off is the story.
\end{frame}

\begin{frame}{Recap: the cascade}
  \textbf{Each metric fixes the previous one's problem:}
  \begin{enumerate}\small
    \item \textbf{Accuracy} --- simple, fails on imbalance ($97\%$ accuracy, $0\%$ recall).
    \item \textbf{Precision \& recall} --- focus on the positive class. \emph{But threshold-dependent.}
    \item \textbf{F1} --- one number that punishes a P/R gap. \emph{Still threshold-dependent.}
    \item \textbf{ROC AUC} --- threshold-free ranking quality. \emph{Overstates on imbalance.}
    \item \textbf{PR AUC} --- threshold-free \emph{and} imbalance-sensitive: the rare-class metric.
    \item \textbf{Lift / gain} --- the operational ``top-$k$\%'' question under a budget.
  \end{enumerate}
  \vskip 4pt
  \textbf{Plus:} multi-class averaging (macro/micro/weighted).
  \vfill
  \fcolorbox{paramgreen}{paramgreen!8}{\parbox{0.97\linewidth}{\small
    \textbf{One-line takeaway:} report multiple metrics --- always one that matches your data's
    imbalance and your problem's cost structure. \textbf{Next:} threshold tuning --- turning
    these scores into decisions.}}
\end{frame}

\end{document}

% =============================================================
% Provenance:
%   - [12] Classification metrics; cheese factory running example.
%   - Cascade: accuracy -> P/R/F1 -> ROC AUC -> PR AUC -> lift; threshold tuning.
%   - Real figures: py_src/cheese_metrics.py (cm_*.pdf), run with ./ma/Scripts/python.exe.
%   - ROC step-by-step pages embedded from LMU i2ml (CC BY 4.0):
%     _reference/lecture_i2ml/slides-pdf/slides-evaluation-roccurves.pdf p6,8,9,10,11,13
%     -> fig/roc_build_1..6.png.
%   - Type I/II illustration: fig/error_types.jpg (rights held by instructor);
%     links math/25_stat_hypothesis_testing.qmd.
%   - 2026-06-21: removed sklearn code blocks; split setup; added P/R/accuracy icons;
%     renamed L07 -> L12.
%   - 2026-07-07: outline gets the "64" easter-egg (fig/dhol_64.png -> youtu.be/i6OOJVFZuw0);
%     accuracy illustration changed from a confusion-matrix icon to a 100-cell correct/wrong grid;
%     LMU roc_build_*.png cropped to *_crop.png (dropped the decorative logo + footer, zoomed in);
%     PR AUC reworked (area intuition + cm_pr_ap.pdf); lift/gain split into two frames (gain +
%     lift-by-decile, cm_lift_decile.pdf); added a worked macro/micro/weighted example (imbalanced
%     good/moldy/dry). New figs from py_src/cheese_metrics.py: cm_pr_ap, cm_lift_decile.
%   - 2026-07-07 (2): added a top-k accuracy frame (multiclass); rewrote the threshold-tuning
%     section for clarity (added a "three ways to pick the cutoff" overview + plainer titles and
%     lead sentences on cost / Youden / recall-floor); also fixed pre-existing overflow on the
%     precision/recall and sklearn frames. Renamed file
%     L12_classification_metrics -> L12_classification_metrics__threshold_tuning.
%   - 2026-07-07 (3): renamed L12_classification_metrics__threshold_tuning ->
%     12_classification_metrics (dropped the "L" prefix and the threshold suffix); split the
%     whole threshold-tuning section out into 13_threshold_tuning.tex (it was not covered in the
%     metrics lecture). Hook and recap now point forward to that deck instead of calibration.
% =============================================================
