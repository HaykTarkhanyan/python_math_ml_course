\documentclass[aspectratio=169]{beamer}
\input{../preamble}

\usepgfplotslibrary{groupplots, fillbetween}
\usepackage{listings}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{armblue}\bfseries,
  commentstyle=\color{paramgreen}\itshape,
  stringstyle=\color{armred},
  showstringspaces=false,
  language=Python,
  frame=single,
  framerule=0pt,
  backgroundcolor=\color{lightbg},
  numbers=none,
  breaklines=true,
  xleftmargin=4pt,
  xrightmargin=4pt,
}

\title{L01e: Hyperparameter Tuning}
\subtitle{Manual, Grid, Random, and Bayesian Optimization}
\date{}

% Section-divider frame: big title + a "why are we moving here" motivation.
% #1 = section title, #2 = motivation (bridges from the previous method).
\newcommand{\sectionintro}[2]{%
  \begin{frame}
    \vfill
    \begin{center}
      {\color{popblue}\Large\textbf{#1}}\\[12pt]
      \fcolorbox{armblue}{armblue!6}{\parbox{0.84\textwidth}{\centering\small #2}}
    \end{center}
    \vfill
  \end{frame}%
}

\begin{document}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

% ================================================================
\section{Why tune?}

\begin{frame}{Parameters vs.\ hyperparameters}
  \textbf{The simple test:} can you \emph{compute} it from the data alone, or do you have to \emph{choose} it? Compute $\Rightarrow$ parameter. Choose $\Rightarrow$ hyperparameter.
  \vfill
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Parameters $\thetav$} are what the model \emph{learns} from data.
    \begin{itemize}\small
      \item Linear regression: the coefficients (one per feature + intercept).
      \item Decision tree: the split points and feature choices at each node.
      \item Neural net: all the weights and biases.
    \end{itemize}
    \column{0.5\textwidth}
    \textbf{Hyperparameters $\lamv$} are what the model \emph{doesn't} learn - you have to choose them.
    \begin{itemize}\small
      \item Ridge regression: \texttt{alpha} (regularization strength).
      \item Decision tree: \texttt{max\_depth}, \texttt{min\_samples\_leaf}.
      \item Neural net: learning rate, number of layers, batch size, weight decay.
    \end{itemize}
  \end{columns}
  \vfill
  Different hyperparameter choices $\Rightarrow$ different fitted models $\Rightarrow$ different generalization. \textbf{Tuning} = systematically picking $\lamv$ to optimize a validation/CV score.
\end{frame}

\begin{frame}{The bilevel optimization view}
  Tuning is an \emph{outer} optimization wrapping the model's own \emph{inner} optimization.
  \vfill
  \begin{center}
    \begin{tikzpicture}[
      every node/.style={font=\small},
      outer/.style={rectangle, draw=armblue, very thick, rounded corners=4pt, fill=armblue!8, inner sep=8pt, minimum width=11.5cm, minimum height=2.6cm},
      inner/.style={rectangle, draw=paramgreen, very thick, rounded corners=4pt, fill=paramgreen!10, inner sep=6pt},
    ]
      \node[outer] (outer) {};
      \node[anchor=north west, font=\small\bfseries, armblue] at (outer.north west) {\;Outer loop (tuning): pick best $\lamv^*$};
      \node[inner, below=0.5cm of outer.north, anchor=north] (inner) {
        \begin{minipage}{7.5cm}\centering
          \textbf{Inner loop (training):} given $\lamv$, solve
          $\thetah(\lamv) = \argmin_{\thetav}\, \riske(\thetav;\, \lamv)$
        \end{minipage}};
      \node[anchor=south, font=\scriptsize, armblue!80!black] at ([yshift=0.05cm]outer.south) {pass $\lamv$ in $\to$ get back $\thetah(\lamv)$, score it on validation/CV $\to$ outer search picks next $\lamv$};
    \end{tikzpicture}
  \end{center}
  \vfill
  \footnotesize \textbf{In plain English:} the outer loop tries hyperparameter values; the inner loop fits the model with those values. \emph{One outer step = one full model training.}
  \vskip 4pt
  Choices on outer search strategy:
  \begin{itemize}\small
    \item \textbf{Manual} - you propose $\lamv$ values from intuition.
    \item \textbf{Grid} - exhaustive over a Cartesian product.
    \item \textbf{Random} - sample uniformly from distributions.
    \item \textbf{Bayesian} - build a surrogate model of score vs.\ $\lamv$, query smartly.
  \end{itemize}
\end{frame}

% ================================================================
\section{Manual tuning}

\sectionintro{Manual tuning}{%
  We know tuning means searching for the best $\lamv$. The simplest search needs no tooling at all - a human proposes a value, reads the validation score, and adjusts. We start here to build intuition before automating.}

\begin{frame}{Manual tuning: ``grad student descent''}
  The simplest strategy: a human proposes one $\lamv$, evaluates, then proposes another based on what they saw.
  \vfill
  Typical workflow:
  \begin{enumerate}
    \item Start with the library's defaults. Get a baseline.
    \item Identify the most-likely-impactful hyperparameter (domain knowledge or quick experiments).
    \item Sweep it across $\sim 5$ values, plot validation score, pick the best.
    \item Fix that value, move to the next hyperparameter, repeat.
    \item Stop when changes don't improve validation score.
  \end{enumerate}
  \vfill
  Also called \emph{manual coordinate descent} - you adjust one hyperparameter at a time.
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.96\textwidth}{\small
    \textbf{Manual is fast to start and slow to scale.} It works for 1-2 HPs and an experienced practitioner. For everything else, the automated methods coming up are usually better. Treat the rest of this deck as upgrades, not replacements.
  }}
\end{frame}

\begin{frame}{When manual works (and when it doesn't)}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \textbf{Manual is best when:}
    \begin{itemize}\small
      \item You have strong intuition about the model (you've trained 50 Ridge regressions already).
      \item The search space is small (1-2 hyperparameters).
      \item Each evaluation is slow (you don't want to launch 100 jobs by accident).
      \item You're \emph{developing} a sense for the model, not just optimizing.
    \end{itemize}
    \column{0.5\textwidth}
    \textbf{Manual breaks down when:}
    \begin{itemize}\small
      \item More than $\sim 3$ hyperparameters interact (you can't visualize the surface).
      \item You can't afford to be inconsistent across runs.
      \item Multiple people work on the same model (reproducibility).
      \item You're shipping to production and need a defensible methodology.
    \end{itemize}
  \end{columns}
  \vfill
  Manual tuning teaches you the most per evaluation but scales the worst.
\end{frame}

\begin{frame}{Pitfalls of manual tuning}
  \begin{enumerate}
    \item \textbf{Confirmation bias.} You notice the run that worked, forget the three that didn't. Mitigation: write down every run, including failures.
    \item \textbf{Validation-set abuse.} The 50th hyperparameter setting you tried still gets evaluated on the same validation set. You're effectively overfitting validation. Mitigation: cross-validation, separate test set, count your evaluations.
    \item \textbf{No reproducibility.} ``I'll try \texttt{alpha=0.3} next... or was it 0.03?'' Mitigation: a notebook log, or skip manual and use a configured automated search.
    \item \textbf{Local optimum.} Coordinate descent gets stuck. The best Ridge \texttt{alpha} \emph{given the current data preprocessing} may differ from the best after you change features. Mitigation: redo the whole sweep occasionally.
  \end{enumerate}
\end{frame}

% ================================================================
\section{Grid search}

\sectionintro{Grid search}{%
  Manual tuning doesn't scale and isn't reproducible - two people get two answers. Let's hand the search to the computer: list candidate values for each hyperparameter and evaluate \emph{every} combination, systematically.}

\begin{frame}{Grid search: exhaustive over a Cartesian product}
  Pick a finite list of candidate values for each hyperparameter. Evaluate \emph{every} combination.
  \vfill
  Example for Ridge with polynomial features:
  \begin{center}\small
    \begin{tabular}{ll}
      Hyperparameter & Candidate values \\
      \hline
      \texttt{alpha} & $\{10^{-3}, 10^{-2}, 10^{-1}, 1, 10, 100\}$ \\
      \texttt{poly\_degree} & $\{1, 2, 3, 4, 5\}$ \\
    \end{tabular}
  \end{center}
  \vfill
  Total: $6 \times 5 = 30$ configurations. With 5-fold CV, $30 \times 5 = 150$ model fits.
  \vfill
  Exhaustive, reproducible, embarrassingly parallel. Also potentially \emph{very} expensive.
\end{frame}

\begin{frame}{Grid search: combinatorial blowup}
  Suppose you tune a model with 4 hyperparameters, 5 candidate values each, using 5-fold CV. \textbf{Try to predict} before reading on:
  \begin{itemize}
    \item How many model fits will grid search require?
    \item If each fit takes 30 seconds, how long does the whole search take (single-threaded)?
  \end{itemize}
  \vfill
  Answers:
  \begin{itemize}
    \item $5^4 \times 5 = 625 \times 5 = \mathbf{3125}$ model fits.
    \item $3125 \times 30\text{s} \approx 26$ hours single-threaded.
  \end{itemize}
  \vfill
  Add one more hyperparameter (5 values): $\mathbf{15{,}625}$ fits, $\mathbf{5.4}$ days. The curse of dimensionality strikes early.
  \vfill
  \textbf{Grid search is impractical above 3 hyperparameters with non-trivial ranges.} The next section introduces random search, which solves this.
\end{frame}

\begin{frame}[fragile]{Code: \texttt{GridSearchCV} with Pipeline}
\begin{lstlisting}
from sklearn.model_selection import GridSearchCV
from sklearn.preprocessing import PolynomialFeatures, StandardScaler
from sklearn.linear_model import Ridge
from sklearn.pipeline import Pipeline

pipe = Pipeline([("poly", PolynomialFeatures()),
                 ("scale", StandardScaler()),
                 ("ridge", Ridge())])

grid = {
    "poly__degree": [1, 2, 3, 4, 5],
    "ridge__alpha": [1e-3, 1e-2, 1e-1, 1, 10, 100],
}

gs = GridSearchCV(pipe, grid, cv=5,
                  scoring="neg_mean_squared_error", n_jobs=-1)
gs.fit(X_train, y_train)
print(gs.best_params_, -gs.best_score_)
\end{lstlisting}
  \vfill
  \footnotesize \texttt{n\_jobs=-1} uses all CPU cores. Parameters are accessed as \texttt{step\_name\_\_param\_name}.
\end{frame}

\begin{frame}{When grid is OK}
  \begin{itemize}
    \item $\leq 2{-}3$ hyperparameters AND each has $\leq 6$ candidate values.
    \item Each model fit is cheap (small data, simple model).
    \item Reproducibility / publication: ``we evaluated all $X$ configurations.''
    \item \textbf{Discrete, narrow, well-known values} (e.g., \texttt{max\_depth} $\in \{3, 5, 7\}$ for trees) - there's nothing to randomize over.
    \item Inputs have clearly preferred discrete values from prior knowledge.
  \end{itemize}
  \vfill
  Once you have 4+ hyperparameters or 10+ values each, grid stops being practical. Random search is almost always better.
\end{frame}

% ================================================================
\section{Random search}

\sectionintro{Random search}{%
  Grid search explodes combinatorially and spends equal budget on axes that barely matter. What if, instead of a rigid lattice, we sampled each hyperparameter at random - so \emph{every} trial probes a fresh value on \emph{every} axis?}

\begin{frame}{Why grid wastes compute in high dimensions}
  Most hyperparameters don't matter equally. For a typical Ridge + polynomial model:
  \begin{itemize}
    \item \texttt{alpha} matters a lot.
    \item \texttt{poly\_degree} matters a lot.
    \item Other small dials (e.g., \texttt{tol}, \texttt{max\_iter}) barely affect the score.
  \end{itemize}
  \vfill
  Grid search spends \emph{equal} compute on each axis. If $9$ out of $10$ axes are flat, you've used 90\% of your budget exploring flat directions.
  \vfill
  \textbf{Random search} samples each hyperparameter from a distribution. Every trial visits a new value on \emph{every} axis - including the ones that matter.
\end{frame}

\begin{frame}{Bergstra--Bengio: random $>$ grid in high dimensions}
  \begin{columns}[T]
    \column{0.5\textwidth}
    \begin{center}
      \textbf{Grid search (3x3)}\\[4pt]
      \begin{tikzpicture}[scale=1]
        \draw[gray!50] (0,0) rectangle (3,3);
        \foreach \x in {0.5, 1.5, 2.5}
          \foreach \y in {0.5, 1.5, 2.5}
            \fill[armblue] (\x, \y) circle (3pt);
        % projection lines
        \foreach \x in {0.5, 1.5, 2.5}
          \draw[armred, dashed, thick] (\x, 0) -- (\x, -0.3);
        \node[font=\scriptsize] at (1.5, -0.7) {only 3 unique x values};
        \node[font=\tiny, anchor=east] at (0, 1.5) {y};
        \node[font=\tiny, anchor=north] at (1.5, 0) {x};
      \end{tikzpicture}
    \end{center}
    \column{0.5\textwidth}
    \begin{center}
      \textbf{Random search (9 samples)}\\[4pt]
      \begin{tikzpicture}[scale=1]
        \draw[gray!50] (0,0) rectangle (3,3);
        \foreach \x/\y in {0.3/2.1, 0.6/0.8, 1.0/2.6, 1.3/1.4, 1.7/0.4,
                           2.0/2.2, 2.3/1.1, 2.6/2.5, 2.9/0.7}
          \fill[armblue] (\x, \y) circle (3pt);
        \foreach \x in {0.3, 0.6, 1.0, 1.3, 1.7, 2.0, 2.3, 2.6, 2.9}
          \draw[armred, dashed, thick] (\x, 0) -- (\x, -0.3);
        \node[font=\scriptsize] at (1.5, -0.7) {9 unique x values};
        \node[font=\tiny, anchor=east] at (0, 1.5) {y};
        \node[font=\tiny, anchor=north] at (1.5, 0) {x};
      \end{tikzpicture}
    \end{center}
  \end{columns}
  \vfill
  \footnotesize Random search gets 9 distinct samples along the important axis; grid gets 3. The wider the gap between effective dimensionality and total dimensionality, the bigger the random-search win.
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.96\textwidth}{\small
    \textbf{Default: random search for $\geq 3$ hyperparameters.} With $\sim 60$ trials, random search typically matches a $10^d$ grid. Reach for grid only when you have specific known values.
  }}
\end{frame}

\begin{frame}[fragile]{Code: \texttt{RandomizedSearchCV}}
\begin{lstlisting}
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import loguniform, randint

dist = {
    "ridge__alpha":   loguniform(1e-4, 1e3),   # continuous, log-scale
    "poly__degree":   randint(1, 7),           # discrete 1..6
}

rs = RandomizedSearchCV(pipe, dist, n_iter=60, cv=5,  # n_iter = budget
        scoring="neg_mean_squared_error",
        random_state=509, n_jobs=-1)
rs.fit(X_train, y_train)
print(rs.best_params_, -rs.best_score_)
\end{lstlisting}
  \vfill
  \scriptsize \texttt{loguniform}/\texttt{uniform}/\texttt{randint} for log-scale / bounded / discrete HPs. \textbf{Why random is nice:} stop anytime, bound the budget with \texttt{n\_iter}, mixes with continuous distributions. Modern alt: \texttt{HalvingRandomSearchCV}.
\end{frame}

% ================================================================
\section{Bayesian optimization \& beyond}

\sectionintro{Bayesian optimization \& beyond}{%
  Random search throws \emph{independent} darts: each trial ignores everything the previous trials revealed. We'd rather choose each new experiment \emph{using the results so far} - spending trials where the score already looks promising, not blindly.}

\begin{frame}{Bayesian optimization (and other smart searches)}
  Random search treats every trial as independent. \textbf{Bayesian optimization (BO)} instead \emph{learns} from the scores seen so far: it fits a probabilistic \textbf{surrogate} of score-vs-$\lamv$, then picks the next $\lamv$ that best trades off \emph{exploit} (where the mean looks high) against \emph{explore} (where it's still uncertain). Every trial uses information from all the previous ones.
  \vfill
  \begin{itemize}\small
    \item \textbf{Tools:} Optuna (TPE sampler - the modern default), Hyperopt, Ray Tune. They ``just work'' across mixed (float / int / categorical) spaces.
    \item \textbf{Worth it when} each evaluation is expensive ($\gtrsim 1$ min) \emph{and} you have $\geq 4$ interacting HPs. Cheap evals, or a big parallel cluster $\to$ plain random search is simpler and nearly as good.
  \end{itemize}
  \vfill
  \fcolorbox{armblue}{armblue!6}{\parbox{0.96\textwidth}{\small
    \textbf{We already built this machinery in the optimization module.} The full theory - surrogate models, Gaussian processes, acquisition functions (EI / UCB / PI) - is in \textbf{Math 14: Bayesian Optimization}. \\[3pt]
    \textbf{Evolutionary algorithms} (CMA-ES, genetic algorithms) are another derivative-free way to tune: population-based, gradient-free, strong on rugged / non-differentiable spaces - see \textbf{Math 13: Evolutionary Algorithms}.
  }}
\end{frame}

% ================================================================
\section{In practice}

\sectionintro{In practice}{%
  We now have four search methods. But which one you pick matters less than \emph{how} you use it: how you shape the search space, how you avoid burning compute, and what a real run actually looks like.}

\begin{frame}{Designing the search space}
  The method matters less than the \emph{space} you hand it. Three rules:
  \vfill
  \begin{enumerate}\small
    \item \textbf{Right scale.} \emph{Log-uniform} for anything spanning orders of magnitude (learning rate, regularization $\lambda$); \emph{uniform} for bounded ratios (\texttt{l1\_ratio}, momentum). Linear-spacing $\lambda$ as $\{0.001, 0.01, 0.1\}$ wastes almost the whole range.
    \item \textbf{Right type.} Integer for counts (\texttt{max\_iter} iterations, polynomial degree); float for continuous; categorical for unordered choices (penalty L1/L2, solver).
    \item \textbf{Bracket the optimum.} Ranges wide enough that the best value lands \emph{inside}, not at an endpoint.
  \end{enumerate}
  \vfill
  \begin{center}\scriptsize
  \begin{tabular}{ll}
    Hyperparameter kind & Distribution \\
    \hline
    learning rate, regularization $\lambda$ (\texttt{alpha}) & log-uniform \\
    \texttt{l1\_ratio} (ElasticNet), momentum & uniform (bounded) \\
    iterations (\texttt{max\_iter}), polynomial degree & integer \\
    penalty (L1 / L2), solver & categorical \\
  \end{tabular}
  \end{center}
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.96\textwidth}{\small
    \textbf{The \#1 search mistake:} a range that doesn't contain the optimum -- if your best value sits at a boundary, widen and re-run. \textbf{Naming gotcha:} sklearn calls $\lambda$ \texttt{alpha}, and you search it on a \emph{log} scale. \emph{Conditional} HPs (\texttt{l1\_ratio} only matters for ElasticNet, not Ridge) need conditional spaces - Optuna handles them naturally.
  }}
\end{frame}

\begin{frame}[fragile]{Specifying the space: Optuna vs Hyperopt}
  Same job, two APIs -- tuning a regularized linear model on $\lambda$ (\texttt{alpha}) and the iteration budget (\texttt{max\_iter}). \textbf{Optuna} is \emph{define-by-run} (sample inside the objective); \textbf{Hyperopt} is \emph{define-then-run} (a space dict up front).
  \vfill
  \begin{columns}[T]
    \column{0.5\textwidth}
    \centering\scriptsize \textbf{Optuna} - \texttt{trial.suggest\_*}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
def objective(trial):
  alpha = trial.suggest_float(
      "alpha", 1e-4, 1e3, log=True)
  n_iter = trial.suggest_int(
      "max_iter", 100, 5000)
  model = Lasso(alpha=alpha,
                max_iter=n_iter)
  return cv_score(model)
\end{lstlisting}
    \column{0.5\textwidth}
    \centering\scriptsize \textbf{Hyperopt} - \texttt{hp.*}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
space = {
 "alpha": hp.loguniform(
    "alpha", log(1e-4), log(1e3)),
 "max_iter": hp.uniformint(
    "max_iter", 100, 5000),
}
# fmin(objective, space,
#      algo=tpe.suggest, max_evals=60)
\end{lstlisting}
  \end{columns}
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.96\textwidth}{\scriptsize
    \textbf{Gotcha:} Optuna's \texttt{log=True} takes the \emph{raw} bounds; Hyperopt's \texttt{hp.loguniform} takes them in \emph{log space} (\texttt{np.log(low), np.log(high)}). Mixing these up is a classic silent bug. Drive them with \texttt{study.optimize(objective, n\_trials=60)} or \texttt{fmin(fn, space, algo=tpe.suggest, max\_evals=60)}.
  }}
\end{frame}

\begin{frame}[fragile]{Pruning: stop losing trials early}
  Most trials are clearly losing well before they finish. \textbf{Pruning} kills them early and reallocates the budget. Two levels:
  \begin{itemize}\small
    \item \textbf{Model-level early stopping.} For boosted trees / NNs, stop adding rounds / epochs once a validation metric stalls (\texttt{early\_stopping\_rounds}, Keras \texttt{EarlyStopping}). Cuts \emph{each} trial's cost.
    \item \textbf{Search-level pruning.} Kill an underperforming \emph{trial} mid-training. Optuna: \texttt{MedianPruner} (drop trials below the running median), \texttt{HyperbandPruner} (multi-fidelity).
  \end{itemize}
  \vfill
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
study = optuna.create_study(pruner=optuna.pruners.MedianPruner())
def objective(trial):
    for step in range(100):              # epochs / boosting rounds
        val = train_one_more_step()
        trial.report(val, step)
        if trial.should_prune():         # below the median so far?
            raise optuna.TrialPruned()
    return val
\end{lstlisting}
  \vfill
  \footnotesize Pruning + early stopping commonly cut search time 2-5$\times$ - biggest win when training has a natural ``step'' (epochs, rounds).
\end{frame}

\begin{frame}{Benchmark: which points does each method try?}
  \footnotesize Tune 2 knobs of a boosted-tree model (a black box; trees come in ch.3) on \texttt{diabetes}: \texttt{n\_estimators} (iteration count) and \texttt{reg\_lambda} ($\lambda$). Same \emph{56-trial} budget, 5-fold CV; $\star$ = best.
  \vskip 2pt
  \begin{center}
    \includegraphics[width=0.96\textwidth]{fig/l01e_hp_search_patterns.pdf}
  \end{center}
  \vskip 1pt
  \fcolorbox{armred}{armred!8}{\parbox{0.98\textwidth}{\scriptsize
    \textbf{The sampling strategy is the whole story.} \textbf{Grid} fills a rigid lattice; \textbf{random} scatters uniformly; \textbf{Optuna} is \emph{adaptive} - early trials (dark) explore widely, late trials (yellow) cluster on the good region. Here it also reached the best score (test RMSE 55.4 vs 56.3 / 57.3) and the shortest search time (next slide).
  }}
\end{frame}

\begin{frame}{Benchmark: score and cost}
  \footnotesize Same 56-trial runs, summarized: best held-out test RMSE (predictive performance) and total search time per method.
  \vskip 2pt
  \begin{center}
    \includegraphics[width=0.72\textwidth]{fig/l01e_hp_benchmark_bars.pdf}
  \end{center}
  \vskip 2pt
  \fcolorbox{armred}{armred!8}{\parbox{0.97\textwidth}{\scriptsize
    \textbf{Equal budget, different payoff.} All three do the same 280 fits and land close on accuracy (RMSE 55-57) - on a small 2-HP space no method works magic. Optuna is fastest: it spends most trials on cheap, small-\texttt{n\_estimators} models and skips the expensive big ones grid and random keep evaluating. (Wall-clock is single-threaded and machine-dependent - the \emph{ordering} is the point, not the exact seconds.)
  }}
\end{frame}

\begin{frame}[fragile]{A practical workflow}
  \begin{enumerate}\small
    \item \textbf{Pick a few HPs.} Tune the 2-3 that matter most - not everything at once.
    \item \textbf{Get a feel by hand.} Try a handful of values manually to see roughly where the good region is and what each knob does.
    \item \textbf{Set ranges from that.} Use what you learned to pick (log-)ranges that bracket the good region.
    \item \textbf{Then automate.} Launch a Bayesian search and let it run while you have lunch.
    \item \textbf{Save progress.} Persist the study so you can pause, resume, and keep optimizing overnight.
  \end{enumerate}
  \vskip 2pt
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
study = optuna.create_study(
    study_name="ridge_tuning",
    storage="sqlite:///optuna.db",  # persist trials to disk
    load_if_exists=True)            # resume the saved study
study.optimize(objective, n_trials=50)  # rerun anytime to add more
\end{lstlisting}
  \vskip 2pt
  \fcolorbox{armred}{armred!8}{\parbox{0.97\textwidth}{\small
    \textbf{New to a model? Do lots of manual search first.} You learn far more per trial by hand - what each knob does, what ``good'' looks like - than by watching an automated search you don't yet understand. Automate once you have the intuition.
  }}
\end{frame}

% ================================================================
\section{Wrap-up}

\begin{frame}{The compute budget reality}
  Hyperparameter tuning consumes most of an ML project's compute. Practical patterns:
  \vfill
  \begin{itemize}
    \item \textbf{Subsample.} Tune on 10\% of training data, retrain best config on full data. Often within 1\% of fully-tuned.
    \item \textbf{Early stopping + pruning.} Stop weak trials before they finish - within-model early stopping and search-level pruning (see the pruning frame). Cuts each trial's cost 2-5x.
    \item \textbf{Multi-fidelity methods.} Hyperband / Successive Halving: run many trials cheaply, gradually allocate more budget to the survivors.
    \item \textbf{Stop when score plateaus.} Track best-so-far; if it hasn't improved in 20 trials, you're done.
  \end{itemize}
  \vfill
  \fcolorbox{armred}{armred!8}{\parbox{0.96\textwidth}{\small
    \textbf{Honest reporting:} the score you tuned to optimize is \emph{optimistically biased}. Report the test-set score after tuning is complete (or use nested CV - L05b). \emph{Conceptually: HP tuning navigates the bias-variance tradeoff (L01d2). Each HP setting moves the model along the U-shape; CV finds the sweet spot empirically.}
  }}
\end{frame}

\begin{frame}{Decision flowchart}
  \begin{center}
    \begin{tikzpicture}[
      node distance=0.45cm and 0.7cm,
      box/.style={rectangle, draw=armblue, very thick, text width=2.9cm, align=center, font=\scriptsize, rounded corners=2pt, fill=armblue!5, minimum height=0.85cm},
      result/.style={rectangle, draw=paramgreen, very thick, text width=2.7cm, align=center, font=\scriptsize, rounded corners=2pt, fill=paramgreen!10, minimum height=0.85cm},
      arr/.style={->, >=stealth, thick, armblue},
    ]
      \node[box] (start) {How many hyperparameters?};
      \node[result, right=of start] (manual) {Manual or grid (1-2 HPs)};
      \node[box, below=of start] (expensive) {Each evaluation expensive (mins+)?};
      \node[result, right=of expensive] (bo) {Bayesian opt (Optuna TPE)};
      \node[box, below=of expensive] (parallel) {Heavily parallel hardware?};
      \node[result, right=of parallel] (rs) {Random search};
      \node[result, below=of parallel] (rs2) {Random search (default)};

      \draw[arr] (start) -- node[above, font=\tiny]{$\leq 2$} (manual);
      \draw[arr] (start) -- node[left, font=\tiny]{$\geq 3$} (expensive);
      \draw[arr] (expensive) -- node[above, font=\tiny]{yes} (bo);
      \draw[arr] (expensive) -- node[left, font=\tiny]{no} (parallel);
      \draw[arr] (parallel) -- node[above, font=\tiny]{yes} (rs);
      \draw[arr] (parallel) -- node[left, font=\tiny]{no} (rs2);
    \end{tikzpicture}
  \end{center}
  \vfill
  \footnotesize Defaults: $\leq 2$ HPs $\to$ grid is fine; otherwise random search; if compute is the bottleneck, Bayesian opt. \\[2pt]
  \textbf{Whatever method:} use subsampling, early stopping, and pruning to multiply your effective budget (see compute-budget frame).
\end{frame}

\begin{frame}{Recap}
  \begin{itemize}\small
    \item \textbf{Parameters} are learned from data. \textbf{Hyperparameters} are chosen by you. Tuning $=$ outer optimization wrapping the inner fit.
    \item \textbf{Manual.} Cheap, intuition-building, doesn't scale. Watch for confirmation bias and validation-set abuse.
    \item \textbf{Grid.} Exhaustive over Cartesian product. Fine for $\leq 2$ HPs; explodes in high dimensions.
    \item \textbf{Random.} Default for $\geq 3$ HPs. Each trial is a fresh sample on every axis. Easy to budget and parallelize. Beats grid when effective dimensionality is low.
    \item \textbf{Bayesian \& beyond.} Models the score-vs-HP surface and queries smartly; best when each evaluation is expensive. Optuna's TPE is the modern default. Full theory in \textbf{Math 14} (Bayesian opt) and \textbf{Math 13} (evolutionary algorithms).
    \item \textbf{In practice.} Design the space well (log-uniform, bracket the optimum), prune weak trials, and remember equal-budget benchmarks show the win is often \emph{speed}, not accuracy.
    \item \textbf{Bias-variance lens (L01d2):} HP tuning navigates the bias-variance U. Each candidate setting moves the model along the U; CV finds the sweet spot. Without this lens HP tuning feels arbitrary; with it, it's purposeful.
    \item \textbf{Honest reporting.} Tuning inflates the score; report on a held-out test set or use nested CV (L05b).
  \end{itemize}
\end{frame}

\end{document}

% =============================================================
% Provenance:
%   - Topics: manual, grid, random HP tuning + brief Bayesian/evolutionary.
%   - Follows L01d evaluation protocol naturally: CV is the scorer
%     that all tuning methods rely on.
%   - Bayesian opt section collapsed to one pointer frame; full theory
%     deferred to Math 14 (Bayesian opt) and Math 13 (evolutionary algs).
%   - "In practice" section: search-space design (alpha=lambda + max_iter,
%     covered concepts only), Optuna/Hyperopt API (params verified via
%     Context7, 2026-06), pruning, and a real benchmark.
%   - Benchmark visualizes WHICH (n_estimators, reg_lambda) points each
%     method samples on diabetes; Optuna coloured by trial order to show
%     adaptive concentration. Generated by py_src/hp_search_benchmark.py
%     (writes fig/l01e_hp_search_patterns.pdf); numbers logged to logs/.
%     XGBoost kept here only as a 2-knob black box (trees land in ch.3).
%   - Forward reference to L05b nested CV.
%   - \pause removed per user request (overrides feedback_predict_first_slides);
%     HW frame removed per user request.
% =============================================================
